Good. This narrows it a lot.

## What is now true

The problem is **not** ballot acceptance itself.

You now have evidence that:

* tickets are sent
* tickets are observed
* ballots are submitted
* Rust accepts ballots
* Rust rejects none
* the main failure is **observability and identity mapping**, not the acceptance reducer

Your findings imply three distinct layers:

1. **Protocol truth**

   * Rust ballot reducer is accepting ballots.
2. **Coordinator UI truth**

   * mapping accepted ballots back to the intended voter row is weak.
3. **Harness truth**

   * success metric is still reading the wrong source.

So the next work should be:

> **make accepted ballots carry stable lineage, map them correctly, and make the harness read coordinator-authoritative acceptance.**

That is the right order.

# Next phases

## Phase 1 - Fix harness truth first

### Goal

Make the harness report the same acceptance truth that Rust already has.

### Why

Right now the harness is still reading voter-side `ballotAccepted`, which is really publish success, not authoritative acceptance.

That makes every run look worse or more ambiguous than it really is.

### Change

In `web/scripts/live-simple-relay-check.js`:

* stop using voter debug `ballotAccepted` as the primary accepted metric
* read coordinator-authoritative values instead:

  * `acceptedBallotCount`
  * `rejectedBallotCount`
  * `rejectedBallots[]`

### New success fields

Add to the harness summary:

* `coordinatorAcceptedBallots`
* `coordinatorRejectedBallots`
* `coordinatorAcceptedByLineage`
* `voterPublishedBallots`
* `voterObservedTickets`

### Success rule for this 1/25/1 deployment

Primary success metric should be:

```text id="aw7edp"
success = acceptedBallotCount >= expectedAcceptedThreshold
```

Not:

```text id="swm5it"
success = voterDebug.ballotAccepted
```

### Exit condition

The harness reports accepted ballots from coordinator-authoritative Rust state.

---

## Phase 2 - Add stable ballot lineage to the Rust schema

### Goal

Make each accepted ballot attributable to the intended request/ticket lineage.

### Why

Right now `EncryptedBallotEvent` lacks:

* `request_id`
* `ticket_id`

and `author_pubkey` is an ephemeral ballot key, not enough to map back to the intended voter/request cleanly.

That is the main structural gap now.

### Change

In `auditable-voting-core/src/event.rs`:

Add fields to `EncryptedBallotEvent` payload:

* `request_id: Option<String>`
* `ticket_id: Option<String>`

Prefer non-optional in the new path, but if you need compatibility with old events, make them optional first and then enforce them in the new flow.

### Exact rule

For new ballot submissions:

* ballot must carry canonical `request_id`
* ballot must carry canonical `ticket_id`

### Compatibility rule

For older events without those fields:

* accept them if current compatibility rules allow
* but do not rely on them for precise voter-row mapping

### Exit condition

Every newly submitted ballot carries stable lineage fields.

---

## Phase 3 - Bridge the new ballot lineage through TS

### Goal

Actually populate the Rust ballot event with the stable lineage.

### Change

Find the ballot submission builder in TS and ensure it includes:

* `request_id`
* `ticket_id`
* `round_id`
* `election_id`

If there is already a ballot payload object, extend it.

### Required files

Likely:

* ballot submission code path
* bridge into Rust protocol event creation
* any TS type for encrypted ballot payload

### Important rule

Use the **same canonical ids** already used in:

* request
* ticket delivery
* completion tracking

Do not generate new lineage values here.

### Exit condition

A ballot submitted from the UI reaches Rust with the same `request_id` / `ticket_id` lineage as the ticket that enabled it.

---

## Phase 4 - Make coordinator follower-row mapping use lineage, not ephemeral pubkey

### Goal

Show the correct voter row as accepted.

### Why

The current weak point is:

* Rust accepted ballot exists
* coordinator UI cannot confidently match it to the correct follower row because it only has ephemeral `author_pubkey`

### Change

In `web/src/SimpleCoordinatorApp.tsx`:

Replace any matching logic that tries to infer acceptance from:

* `author_pubkey`
* submitted-ballot presence
* publish success

with lineage matching:

* `request_id`
* `ticket_id`

### Mapping rule

For each follower row, derive the canonical expected lineage:

* follower row -> request id
* request id -> ticket id
* accepted ballot carrying matching lineage -> row accepted

### Add debug output

Expose on coordinator debug:

* `acceptedBallotsByRequestId`
* `acceptedBallotsByTicketId`
* `unmatchedAcceptedBallots`
* `rowsWithoutAcceptedBallot`

### Exit condition

Coordinator UI row mapping is driven by Rust-accepted ballot lineage, not by ephemeral ballot pubkey or local submission state.

---

## Phase 5 - Make “delivery confirmed by ballot” explicit in the coordinator state

### Goal

Finish the simplification you wanted:

* ballot acceptance is the authoritative completion signal.

### Change

In `SimpleCoordinatorApp.tsx`, `simpleVotingSession.ts`, and/or the relevant protocol state service:

Add:

* `ticketDeliveryConfirmedByBallot`
* `ticketDeliveryConfirmedByAck`
* `ticketDeliveryConfirmed`

Use:

```text id="i0ej2a"
ticketDeliveryConfirmed =
  ticketDeliveryConfirmedByAck || ticketDeliveryConfirmedByBallot
```

But for this deployment, treat:

* `ticketDeliveryConfirmedByBallot`
  as the primary signal.

### UI wording

Change any:

* `Waiting for receipt acknowledgement`

to:

* `Waiting for acknowledgement or accepted ballot`
  or simply:
* `Waiting for completion confirmation`

### Exit condition

Missing ack no longer makes a successfully accepted voter appear incomplete.

---

## Phase 6 - Add a strict rejection/acceptance audit view

### Goal

Make future debugging trivial.

### Change

Keep the new fields you added and extend them slightly:

* `acceptedBallotCount`
* `rejectedBallotCount`
* `rejectedBallots[]`

Add:

* `acceptedBallots[]`

  * with `request_id`
  * `ticket_id`
  * `round_id`
  * `event_id`
  * optional `author_pubkey`

### Why

Then every failed harness run can say:

* ballots published
* ballots accepted
* ballots unmatched to UI rows
* ballots rejected and why

### Exit condition

No more ambiguity between:

* accepted in Rust
* displayed in coordinator UI
* counted by harness

---

## Phase 7 - Re-run 1 / 5 / 1 before touching scale again

### Goal

Confirm the observability/mapping fix before scaling up.

### Do next

Re-run:

* `1 / 5 / 1`

Expected outcome after the above patches:

* `tickets sent: 5/5`
* `tickets observed: 5/5`
* `ballots submitted: 5/5`
* `coordinatorAcceptedBallots: 5/5`
* row mapping: `5/5`
* harness success: true

### Exit condition

Single-coordinator single-round acceptance is measured correctly end-to-end.

---

## Phase 8 - Then move to 1 / 25 / 1

### Goal

Exercise the actual course-feedback deployment shape.

### Why

Once the acceptance/mapping path is correct, the next real deployment test is:

* `1 coordinator`
* `25 voters`
* `1 round`

This avoids multi-coordinator startup complexity and tests the actual intended first use case.

### Measure

At minimum:

* round seen
* blind key seen
* request sent
* ticket sent
* ticket observed
* ballot submitted
* coordinator accepted ballot count

Ack remains diagnostic only.

---

# Recommended exact order

1. **Fix harness to use coordinator-authoritative acceptance**
2. **Add `request_id` + `ticket_id` to Rust ballot schema**
3. **Populate those lineage fields from TS ballot submission**
4. **Map coordinator follower rows using lineage**
5. **Make ballot acceptance imply delivery confirmed**
6. **Expose accepted ballot details in coordinator debug**
7. **Re-run `1 / 5 / 1`**
8. **Then run `1 / 25 / 1`**

# Short summary

You have now proved:

> **Ballot acceptance happens. The remaining problem is that the app and harness do not observe and map that accepted state correctly.**

So the next work is not protocol redesign again. It is:

> **lineage in the ballot schema, coordinator-row mapping by lineage, and harness success based on coordinator-authoritative acceptance.**

That is the highest-value next tranche.
