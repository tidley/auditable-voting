Good. This is progress again.

## What Phase 22 proved

You now know:

* coordinator-side questionnaire publish is at least being **attempted**
* the new probe path is **live**
* voter-side read diagnostics are present
* current observed failure is still:

  * `questionnaireSeenCount: 0`
  * `definitionEventCount: 0`

So the system is now narrowed to a clean question:

> **Is the questionnaire definition event actually getting onto the relay in a retrievable form, or is the voter read path still querying the wrong thing?**

That is a much better place to be than “nothing works”.

# Most likely remaining bug classes

From what you wrote, the highest-probability buckets are now:

## 1. Publish success is being reported too early or incorrectly

The coordinator says publish attempted, but the event may not actually be available on relays.

## 2. The read filter is wrong

This is very plausible given the earlier mailbox issue history.

Typical causes:

* wrong kind
* wrong `questionnaire_id`
* wrong `etype`
* wrong tag names
* querying before relay indexing catches up

## 3. The relay stores the event but does not return it under the narrow filtered query

Again, very plausible, especially if:

* `kind_only` works
* but `kind + questionnaire_id` or `kind + questionnaire_id + etype` does not

## 4. The UI read path sees data but does not reflect it into debug state

Less likely now, but still possible.

# Best next move

## Phase 23 - force exact write/read equivalence decision

For a single `1/1/1` run, use the new `phase22QuestionnaireProbe` to answer these exact questions:

### A. Does `kind_only` find the definition event?

If no:

* publish is not actually landing, or relay write success is meaningless

If yes:

* publish is probably fine, and the bug is on the read/filter side

### B. Does `kind + questionnaire_id` find it?

If no, but `kind_only` does:

* tag/filter mismatch or relay indexing mismatch

### C. Does `kind + questionnaire_id + etype` find it?

If no, but the broader query does:

* the `etype` filter is too strict or wrong

### D. Does the voter read filter exactly match the published event tags?

Compare literally:

* event kind
* all tags
* queried kinds
* queried tags
* questionnaire id
* etype value

# Practical decision tree

## Case 1 - `kind_only` fails

Then the publish path is lying or the relay is not actually serving the event back.
Next step: inspect publish result handling and relay write confirmation semantics.

## Case 2 - `kind_only` succeeds, filtered queries fail

Then this is almost certainly a **filter/tag mismatch** or **relay tag-indexing mismatch**.
Next step: broaden reader fetch and reconcile locally.

## Case 3 - filtered queries succeed in probe, but UI still shows zero

Then it is a **UI/debug reflection bug** in `QuestionnaireVoterPanel.tsx`.

# What I would implement next

## 1. Add one fallback read mode for questionnaire definition

For diagnosis only:

* first try exact filter
* if zero results, try `kind_only`
* then reconcile locally by:

  * `questionnaire_id`
  * `etype`

That will tell you very quickly whether the exact filter is the problem.

## 2. Log the exact published definition event snapshot in the harness summary

You already have the machinery; make sure it includes:

* event id
* kind
* full tags
* relay success count

## 3. Log the exact voter definition filter snapshot

Include:

* queried kinds
* queried tags
* relays
* result counts

## 4. Prefer broader fetch + local match if relay filtering is flaky

If the probe shows `kind_only` works but tag-filter queries do not, then for questionnaire definition/state in this MVP you should strongly consider:

* fetch broader
* match locally

That is often much more robust.

# Recommended immediate order

1. Run the Phase 22 probe again on `1/1/1`
2. Inspect:

   * `kind_only`
   * `kind + questionnaire_id`
   * `kind + questionnaire_id + etype`
3. If `kind_only` works and filtered fetch fails, implement broad-fetch fallback immediately
4. Re-run `1/1/1`
5. Then move to `1/5/1`

# Short summary

You now have the right probe in place.

The next goal is:

> **determine whether questionnaire definition events are published but not retrievable under the current filter, or not actually retrievable at all.**

My bet, based on the pattern so far, is:

> **the event exists, but the current read filter is too narrow or mismatched, and a broader fetch with local reconciliation will unblock it.**

