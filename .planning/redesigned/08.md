Good. This is a strong result.

You now have a much clearer picture:

* the **scheduler is no longer opaque**
* **first-send priority is working**
* **resend is real**
* **small-run sanity is good again**
* for `1 / 25 / 1`, the dominant miss has now shifted from **`ticket_not_sent`** toward **`ticket_published_not_observed`**, especially once you give the run enough time

That shift is exactly what you wanted. It means the next tranche should stop focusing on queue throughput and move onto the **ticket observation plane**.

# Interpretation of the latest runs

## What the numbers say

### `1 / 25 / 1`, 30s/30s

* `c3`: mostly blocked at `ticket_not_sent`
* `c5`: some real progress, `5` accepted, resend helping
* `c8`: worse, and odd `waiting_for_request:25` behaviour

This says:

* higher concurrency is **not automatically better**
* `5` looks better than `3` or `8`
* there may be a secondary bug or starvation effect at `c8`

### `1 / 25 / 1`, 60s/60s, `c5`

* `ticket_not_sent` drops from `18` to `8`
* `ticket_published_not_observed` rises to `12`
* `started` rises to `18`
* accepted still only `5`

This is the most important run.

It says:

> **When the scheduler gets enough time, it can start many more sends.
> The next bottleneck is no longer mostly “not sent”.
> It is now “sent, but not observed”.**

That is the exact transition you wanted to see.

### `1 / 5 / 1`

* full success
* resend active
* one row recovered by resend

This is good. It means the transport-recovery machinery is not fundamentally broken.

# Next phases

## Phase 8 - attack `ticket_published_not_observed`

## Objective

Make more published tickets actually become visible to voters.

## Why

That is now the dominant failure class once scheduler pressure is reduced.

## Required work

### 8.1 Make observation recovery faster and explicit

For each row where:

* `ticketPublishSucceededAt` exists
* but `ticketObservedLiveAt` and `ticketObservedBackfillAt` are both missing after threshold

do this in order:

1. run bounded mailbox/ticket backfill for that voter
2. if still missing, resend same logical `ticket_id`
3. record whether the recovery came from:

   * backfill
   * resend
   * still missing

### 8.2 Reduce observation delay threshold for recovery

You already lowered retry age for send recovery.
Now do the same for **observation recovery** specifically.

Add a separate knob if needed, for example:

* `SIMPLE_TICKET_OBSERVE_RECOVERY_AGE_MS`

Do **not** reuse one generic timeout for everything.

### 8.3 Add explicit live-vs-backfill-vs-resend counters to the top-level summary

You already have pieces of this. Promote them to the main run summary:

* `ticketObservedLiveCount`
* `ticketObservedBackfillCount`
* `ticketRecoveredByResendCount`
* `ticketStillMissingCount`

## Exit condition

In `1 / 25 / 1`, the dominant miss class is no longer `ticket_published_not_observed`.

---

## Phase 9 - investigate relay visibility semantics

## Objective

Work out whether relay write success is a weak predictor of later observation.

## Why

Your current numbers strongly suggest:

* publish is happening
* but observation is not following reliably

## Required work

### 9.1 Add run-level summary ratios

For each run, print:

* published but not observed
* partial relay success but not observed
* full relay success but not observed
* observed only after backfill
* observed only after resend

### 9.2 Compare by relay target count and relay success count

For unmatched rows, aggregate:

* `ticketRelayTargets.length`
* `ticketRelaySuccessCount`

You want to know:

* are rows with `successCount=1` much worse than rows with `successCount=2+`?
* are some relay subsets consistently bad?

### 9.3 If possible, tag which relay produced later observation

If your current instrumentation allows it, record the relay source of:

* first live observation
* first backfill observation

## Exit condition

You can say whether the problem is mainly:

* weak write confirmation semantics
* weak read subscription coverage
* bad relay subset choice
* all three

---

## Phase 10 - fix the `c8` anomaly

## Objective

Understand why `concurrency=8` got worse and showed `waiting_for_request:25`.

## Why

That is suspicious and may indicate:

* scheduler starvation
* request visibility timing
* a race in row readiness bookkeeping
* over-aggressive concurrency causing stale gating

## Required work

Run one focused diagnostic with:

* `concurrency=8`
* `1 / 25 / 1`
* longer waits
* full queue debug enabled

Check:

* did rows actually have `requestCreatedAtMs`?
* did they become `sendEligible`?
* what exact `sendBlockedReason` aggregate dominated?
* why did the queue think `waiting_for_request:25` while some rows later progressed?

## Exit condition

Either:

* `c8` is explained and discarded
* or fixed and retested

For now, `c5` looks like the best working point.

---

## Phase 11 - lock a provisional operating point

## Objective

Pick the best current runtime settings for the course-feedback deployment.

## Current best candidate

From your runs:

* **concurrency = 5**
* longer window clearly helps

So the provisional operating point should likely be:

* `SIMPLE_TICKET_SEND_MAX_CONCURRENCY=5`
* longer round/ticket waits than 30s for now

## Required work

Do a small matrix:

* `c5`, 30s/30s
* `c5`, 60s/60s
* `c5`, maybe 45s/45s

Goal:

* find the smallest wait window that still gives acceptable observation and acceptance

## Exit condition

You have one default test profile for:

* `1 coordinator`
* `25 voters`
* `1 round`

---

## Phase 12 - re-baseline `1 / 25 / 1` after observation fixes

## Objective

Measure whether the observation-plane work actually improved accepted ballots.

## Required output

For each run:

* `voterObservedTickets`
* `voterPublishedBallots`
* `coordinatorAcceptedBallots`
* `coordinatorAcceptedByLineage`
* `rowsWithoutAcceptedBallotCount`
* unmatched stage counts
* `ticketObservedLiveCount`
* `ticketObservedBackfillCount`
* `ticketRecoveredByResendCount`

## Success criterion

For this initial deployment, the key progress metric is:

> accepted ballots rising as `ticket_published_not_observed` falls

---

# What not to do next

Do **not** jump back to:

* multi-coordinator scale
* proof bundles
* receipt commitments
* Rust mailbox reducer completion
* broader architectural changes

You now have a very specific next bottleneck. Stay on it.

# Recommended exact next order

1. **Promote observation recovery counters to top-level run summary**
2. **Add a separate observation recovery timeout knob**
3. **Make backfill-then-resend for published-not-observed tickets more aggressive**
4. **Analyse relay success vs actual observation**
5. **Investigate and likely ignore/fix the `c8` anomaly**
6. **Lock provisional `c5` operating profile**
7. **Repeat `1 / 25 / 1` until observation, not send initiation, improves materially**

# Short summary

You have completed the **scheduler tranche**.

The next tranche is the **observation tranche**:

> **tickets are now being sent often enough under a longer window; the main remaining problem is that published tickets are not being observed reliably enough by voters.**

That is the clearest next step from here.
