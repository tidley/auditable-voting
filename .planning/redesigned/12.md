That is a strong result, even though it feels annoying.

You now have a very specific answer:

> **publish mailbox derivation is no longer the primary problem for the probed row; the blocker is reader-side mailbox selection/filter usage in live and backfill.**

The key evidence is:

* `publishedMailboxMatchesRequest: true`
* `mailboxMatchesLive: false`
* `mailboxMatchesBackfill: false`
* `rowsWithMailboxIdMismatchNoObservation: 5`
* `backfill_no_events_returned: 5`

So the next tranche should stop touching publish-side derivation and focus entirely on **reader-side mailbox discovery and filtering**.

# What this means

You have ruled out one major branch:

* **not** primarily “writer is publishing to the wrong mailbox”

You have narrowed it to:

* reader picks the wrong mailbox id set
* reader is still carrying stale/extra mailbox ids into live or backfill
* live/backfill query scoping is wrong for the active request lineage
* mailbox filter narrowing is still too aggressive or mis-keyed

# Next phases

## Phase 12 - Fix reader mailbox selection to one canonical active mailbox per request

## Goal

Make live and backfill use the exact same mailbox id that the published ticket used.

## Why

Your diagnostics show:

* publish mailbox matches request
* live/backfill mailbox ids do not match that mailbox

That is now the main bug.

## Required work

### 12.1 Make request lineage the primary lookup key everywhere

For voter-side ticket observation, the key must be:

* `requestId`

not:

* voter + round only
* mailbox id guessed from other local state
* latest pending request by row
* any multi-key fallback

### 12.2 For each pending request, store exactly one canonical mailbox id

For each active request record on the voter side, persist:

* `requestId`
* `roundId`
* `requestMailboxId`
* `ticketReadMailboxId`
* `ticketBackfillMailboxId`

Then define:

```text id="p2ud4g"
canonicalTicketMailboxId = requestMailboxId
```

unless you intentionally split request/ticket mailboxes, which currently you should not.

### 12.3 Replace mailbox set selection with canonical mailbox selection

If current code builds a set/list of mailbox ids for a round, stop doing that on the critical path.

For ticket fetch/backfill, use:

* the mailbox id for the exact pending `requestId`

not:

* all possible mailbox ids for the round
* all mailboxes from broader selected-round state

## Exit condition

For every active request:

* `ticketReadMailboxId === requestMailboxId`
* `ticketBackfillMailboxId === requestMailboxId`

---

## Phase 13 - Remove filter over-broadness and over-cleverness at the same time

## Goal

Make the reader ask one simple, exact question first.

## Why

Right now you have evidence that the read path is not using the right mailbox. The fix is not “more fallback mailboxes”; it is “use the right mailbox first”.

## Required work

### 13.1 Primary live subscription filter

For each active request, subscribe using:

* mailbox ticket kind
* `election_id`
* `round_id`
* the canonical mailbox id for that request

Do not union unrelated mailbox ids into that critical query.

### 13.2 Primary backfill filter

Use the same:

* kind
* election
* round
* exact mailbox id

### 13.3 Diagnostic fallback only

If exact-mailbox backfill returns zero and publish success exists, only then run a fallback query, such as:

* kind only
  or
* kind + election + round

and reconcile locally.

That fallback is for diagnosis and recovery, not the primary path.

## Exit condition

The primary reader filter is exact and deterministic.

---

## Phase 14 - Make request-to-mailbox binding immutable after request creation

## Goal

Stop mailbox identity from drifting once a request exists.

## Why

The easiest way to get `mailboxMatchesLive=false` is if the active request’s mailbox binding changes later or gets recomputed inconsistently.

## Required work

Once a request is created and published:

* freeze its mailbox id in state
* do not recompute it from newer selected-round context
* do not replace it with a later derived value
* do not merge other row mailboxes into its live/backfill path

For the active request object, add:

* `mailboxFrozenAt`
* `mailboxFrozenValue`

And use that frozen value for all later ticket/ack reads.

## Exit condition

Mailbox id for a request is assigned once and never changes.

---

## Phase 15 - Add exact row-level comparison in harness for the chosen request

## Goal

Make it obvious when the wrong mailbox is being queried.

## Required fields

For every unmatched row, print:

* `requestId`
* `requestMailboxId`
* `ticketPublishMailboxId`
* `ticketReadMailboxId`
* `ticketBackfillMailboxId`
* `mailboxIdConsistentPublishRequest`
* `mailboxIdConsistentReadRequest`
* `mailboxIdConsistentBackfillRequest`

Where:

```text id="4xq6kw"
mailboxIdConsistentReadRequest =
  requestMailboxId === ticketReadMailboxId

mailboxIdConsistentBackfillRequest =
  requestMailboxId === ticketBackfillMailboxId
```

## Exit condition

A failed row immediately shows whether read/backfill mailbox drift is still present.

---

## Phase 16 - Re-run `1 / 5 / 1` before touching 25 again

## Goal

Prove the mailbox-read fix at the smallest useful scale.

## Why

Your `1 / 5 / 1` run is now the perfect detector for this bug. It currently shows:

* publish success
* zero tickets observed
* mailbox mismatch everywhere
* backfill no events

That is exactly the run you want to flip first.

## Success criteria

After the reader mailbox fix, for `1 / 5 / 1` you want:

* `rowsWithMailboxIdMismatchNoObservation` to collapse toward zero
* `rowsWithMailboxFilterMismatchNoObservation` to reduce sharply
* `votersWithTickets` to rise above zero
* `backfill_no_events_returned` to reduce
* ideally some tickets observed live, or at least via backfill

## Exit condition

`1 / 5 / 1` no longer fails because the reader is looking in the wrong mailbox.

---

## Phase 17 - Then re-run `1 / 25 / 1`

## Goal

See how much of the larger failure was just the mailbox-read bug.

## What to expect

If this fix is right, `1 / 25 / 1` should improve in this order:

1. fewer mailbox-id mismatches
2. fewer `backfill_no_events_returned`
3. more tickets observed
4. more ballots published
5. more ballots accepted

It may still not be fully good, but the current hard blind spot should shrink.

---

# What not to do next

Do **not**:

* tune concurrency again yet
* widen resend again yet
* revisit proof bundles
* revisit Rust mailbox reducers yet
* revisit multi-coordinator yet

You now have a much sharper immediate task than any of those.

# Concrete next order

1. **Make `requestId -> mailboxId` binding immutable**
2. **Use that exact mailbox id for live and backfill**
3. **Stop using broader selected-round mailbox sets on the critical path**
4. **Keep broad fetch only as fallback**
5. **Re-run `1 / 5 / 1`**
6. **Then re-run `1 / 25 / 1`**

# Short summary

This is progress.

Phase 11 proved:

> **The publish side is using the right mailbox for the probed row.
> The read side is still looking in the wrong mailbox.**

So the next tranche is:

> **fix reader-side mailbox selection to be request-lineage exact and immutable, then retest observation.**
