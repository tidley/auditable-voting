Good. Phase 24 is the right fix.

## What is now true

You have a stable architectural result:

* `1 / 1 / 1` passes on real relays
* questionnaire definition, response, and result summary are all visible
* the relay issue is now clearly understood:

  * `kind_only` works
  * `#questionnaire-id` filtering is unreliable
  * broad fetch + local reconciliation fixes it

That is a proper protocol-level breakthrough.

## Decision

Yes, the next natural step is exactly:

> **Run Phase 25: `1 / 5 / 1` on this path.**

## What to measure in `1 / 5 / 1`

Use the current questionnaire-first metrics and add nothing fancy yet.

Watch:

* `questionnaireSeenCount`
* `questionnaireOpenCount`
* `responsesPublishedCount`
* `acceptedResponsesCount`
* `rejectedResponsesCount`
* `resultSummaryPublished`
* `definitionEventCount`
* `responseEventCount`
* `resultEventCount`

For blind-token mode specifically, also make sure you can see:

* token issuance success count
* token-ready count
* duplicate-nullifier rejection count, if exercised

## Pass criteria for Phase 25

A clean pass is:

* `passed = true`
* `completedRounds = 1`
* `roundSuccess = true`
* `questionnaireSeenCount = 5`
* `responsesPublishedCount = 5`
* `acceptedResponsesCount = 5`
* `resultSummaryPublished = true`

A useful partial result is still fine if:

* all failures are explicit
* and they are no longer “questionnaire invisible” failures

## Most likely next failure classes

If `1 / 5 / 1` does not pass, I would expect one of these:

1. **response publication race**
2. **coordinator acceptance / duplicate handling issue**
3. **result summary publication timing**
4. **blind-token issuance not scaling from 1 voter to 5**

What I do **not** expect now is a return to the old mailbox / round / MLS failure classes.

## Recommendation

Run `1 / 5 / 1` next, unchanged except for the now-correct broad-fetch questionnaire transport.

After that:

* if green, go straight to `1 / 25 / 1`
* if red, classify it in the questionnaire/blind-token path only

## Short summary

Phase 24 gives you a solid foundation.

The protocol path now works for `1 / 1 / 1`, and the relay-index problem has a practical fix.

So the right next move is:

> **Phase 25: scale the questionnaire-first blind-token path to `1 / 5 / 1`.**

