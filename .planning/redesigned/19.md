This does **not** look like a blind-token bug yet.

The run failed with:

* `protocolFailureClass: startup`
* `firstMissingStage: roundSeen`
* `completedRounds: []`

So the new blind-token publish/transport slice was not the thing that timed out. The app never reached the stage where that path could matter.

## Likely bug

The most likely bug is that `LIVE_DEPLOYMENT_MODE=course_feedback` is still driving the harness/UI through the **old round-gated startup path**, so the app is still waiting for a “live round” before the new questionnaire flow becomes active.

In other words:

* the MVP questionnaire/blind-token code exists
* but the runtime still treats success as:

  * coordinator startup
  * round open
  * then voter flow

instead of:

* questionnaire published/fetched
* questionnaire visible
* eligibility/request flow
* blind-token response publish

## What to check first

### 1. `web/src/SimpleUiApp.tsx`

Look for the top-level route/branch that decides whether the voter UI shows:

* old round/ticket flow
* or new questionnaire flow

You likely still have something equivalent to:

```ts
if (!hasLiveRound) {
  return <WaitingForLiveRound />;
}
```

for `course_feedback` mode.

That is the wrong gate for the new protocol.

### 2. `web/src/QuestionnaireVoterPanel.tsx`

Make sure this component is actually mounted in `course_feedback` mode **without** requiring `roundSeen`.

### 3. `web/scripts/live-simple-relay-check.js`

The harness still classifies failure with:

* `firstMissingStage: roundSeen`

That suggests the harness success model is still keyed to the old round lifecycle, not the new questionnaire lifecycle.

## Most likely concrete fix

### In `web/src/SimpleUiApp.tsx`

Add a clean split:

* if deployment mode is the old vote flow, use round/ticket logic
* if deployment mode is `course_feedback`, bypass `roundSeen` gating and render questionnaire mode directly from questionnaire availability

Conceptually:

```ts
const isCourseFeedbackMode = deploymentMode === "course_feedback";

if (isCourseFeedbackMode) {
  return <QuestionnaireVoterPanel ... />;
}

// old round-based flow below
if (!hasLiveRound) {
  return <WaitingForLiveRound />;
}
```

### In `web/src/QuestionnaireCoordinatorPanel.tsx`

Similarly, make sure questionnaire publishing/visibility is not blocked on round-open state.

### In `web/scripts/live-simple-relay-check.js`

Add questionnaire-first stage metrics, for example:

* `questionnaireSeen`
* `questionnaireOpen`
* `eligibilityRequested`
* `blindIssueReceived`
* `responsePublished`

And in `course_feedback` mode, stop using `roundSeen` as the first required stage.

## Why I think this is the bug

Because the result says:

* startup timeout
* first missing stage = `roundSeen`

But your new slice is specifically about:

* questionnaire publish
* questionnaire transport
* blind-token response publishing

Those should not need a live round at all if the new architecture is actually active.

## Recommended immediate patch order

1. `web/src/SimpleUiApp.tsx`

   * bypass `roundSeen` / live-round gating in `course_feedback`
   * mount `QuestionnaireVoterPanel` directly

2. `web/src/QuestionnaireCoordinatorPanel.tsx`

   * ensure questionnaire mode does not wait for round-open state

3. `web/scripts/live-simple-relay-check.js`

   * switch stage model in `course_feedback` mode away from `roundSeen`

4. Re-run `1/5/1`

## What successful next run should look like

The next run should fail or pass on questionnaire-specific stages, not on `roundSeen`.

For example:

* `questionnaireSeen`
* `eligibilityRequested`
* `blindIssueReceived`
* `responsePublished`

If `roundSeen` still appears as the first missing stage after that patch, the new mode still is not actually the active runtime path.

If you paste the top-level branch in `SimpleUiApp.tsx` that chooses between old flow and questionnaire flow, I can rewrite the exact conditional.

