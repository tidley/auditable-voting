Good tranche. The important thing is that you have now removed a large class of **false negatives**:

* harness truth is now coordinator-authoritative
* ballot lineage is in Rust
* row mapping is lineage-driven
* repo-wide `tsc --noEmit` is green
* the remaining failure is now much more honestly a **transport / delivery reliability** issue, not an observability issue

## What the latest live rerun means

For `1 / 5 / 1` with 30s waits:

* `voterObservedTickets: 4`
* `voterPublishedBallots: 4`
* `coordinatorAcceptedBallots: 4`
* `coordinatorAcceptedByLineage: 4`
* `coordinatorRejectedBallots: 0`
* one row still unmatched / incomplete
* `roundSuccess: false` only because the threshold expects full acceptance

So the current bottleneck is now much clearer:

> **one voter still fails somewhere between ticket delivery visibility and ballot publication/acceptance**

Not:

* Rust acceptance logic
* coordinator mapping
* harness truth
* ballot rejection

That is real progress.

# Recommended next step

## Do option 1 first: repeated `1 / 5 / 1`

Before `1 / 25 / 1`, quantify reliability on the now-correct measurement path.

### Why

You just changed the truth source and proved one run can still drop 1 voter.
You need to know whether that is:

* a rare transport blip
* a common 20 percent failure mode
* tied to a specific voter slot / ordering / timeout pattern

If you jump straight to `1 / 25 / 1`, you will get a noisier result before you have a baseline.

## Exact next phase

### Phase 4A - reliability baseline on `1 / 5 / 1`

Run repeated `1 / 5 / 1` with the new truth fields.

### Command shape

Use your repeat harness in `course_feedback` mode with the same 30s waits.

### What to collect per run

At minimum:

* `voterObservedTickets`
* `voterPublishedBallots`
* `coordinatorAcceptedBallots`
* `coordinatorAcceptedByLineage`
* `coordinatorRejectedBallots`
* `rowsWithoutAcceptedBallot`
* which voter row is missing
* first missing stage for the missing row

### What to add if not already present

For each unmatched row, dump:

* `requestId`
* `ticketId`
* `ticketSent`
* `ticketObserved`
* `ballotSubmitted`
* `ballotAccepted`
* whether the row ever appeared in `acceptedBallotsByRequestId`
* whether the row ever appeared in `acceptedBallotsByTicketId`

### Success criterion for this phase

You want to know:

* pass rate across 20+ reruns
* whether the missing voter is random or patterned
* whether the miss is primarily:

  * ticket not observed
  * ballot not published
  * ballot published but not accepted
  * accepted but not row-mapped

Given this tranche, my expectation is that the remaining miss is now more likely **upstream of ballot acceptance**, because accepted and lineage-mapped counts are equal in the sample run.

# After that

## Phase 4B - only then run `1 / 25 / 1`

Run `1 / 25 / 1` only after you have a small reliability baseline.

### Why

If `1 / 5 / 1` is, say, 18/20 good with occasional ticket visibility loss, then `1 / 25 / 1` becomes a meaningful stress test.

If `1 / 5 / 1` is only 50-70 percent reliable, `1 / 25 / 1` will mostly tell you “the same bug scales badly”, which is less useful.

### What to measure in `1 / 25 / 1`

Use the same truth fields:

* `voterObservedTickets`
* `voterPublishedBallots`
* `coordinatorAcceptedBallots`
* `coordinatorAcceptedByLineage`
* `coordinatorRejectedBallots`
* `rowsWithoutAcceptedBallot`

And add:

* histogram of ticket observation delay
* histogram of ballot publication delay
* accepted-by-lineage percentage

### Pass threshold for the first course-feedback deployment

You do not need perfection immediately to learn something useful, but you do need a realistic target.

For first serious viability, I would treat this as promising if:

* no rejections
* most losses occur before ballot publication, not inside Rust acceptance
* accepted-by-lineage tracks accepted ballots exactly
* the miss rate is low enough to attack as a transport problem rather than a protocol correctness problem

# Concrete interpretation of the current state

You have now effectively proven:

### Fixed / improved

* ballot acceptance happens
* lineage mapping works for accepted ballots
* harness now sees Rust truth
* no ballot rejections in the sample run

### Still weak

* one voter can still disappear before accepted ballot
* success still depends on delivery reliability
* transport remains the likely next bottleneck

# Literal next task list

## Next task 1

Run repeated `1 / 5 / 1`.

## Next task 2

For each failed/unmatched row, record exact missing stage:

* ticket not observed
* ballot not submitted
* ballot submitted but not accepted
* accepted but not row-mapped

## Next task 3

If the missing stage is mostly `ticketObserved = false`, work on ticket visibility/retry.
If it is mostly `ballotSubmitted = false`, work on voter-side submission flow.
If it is mostly `acceptedBallots < publishedBallots`, inspect acceptance input lineage.
If it is mostly mapping mismatch, inspect the new lineage bridge.

## Next task 4

After 20+ reruns, run `1 / 25 / 1`.

# Short answer

**Pick option 1 first.**
Repeated `1 / 5 / 1` is the right next phase because you have just fixed truth and mapping, and now need a clean reliability baseline before scaling to `1 / 25 / 1`.

If you want, I can also give you a concrete expected-output template for the repeat runs so you can compare them quickly.
