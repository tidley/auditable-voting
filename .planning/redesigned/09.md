That is a clean result. The bottleneck has moved again, and now it is very specific:

## What the latest runs prove

Under longer windows:

* `ticket_not_sent` drops materially
* `ticket_published_not_observed` becomes the dominant miss
* many misses have **full relay publish success**
* `ticketObservedBackfillCount` is still **0**
* resend fires, but recovery remains weak

So the next conclusion is:

> **Send initiation is no longer the primary bottleneck.
> The main failure is now observation-plane failure after publish success.**

That is exactly the kind of narrowing you wanted.

# What this means technically

You now have strong evidence for three statements:

### 1. Scheduler pressure is real, but no longer first

Longer window reduces `ticket_not_sent`, so the send queue is part of the problem, but not the dominant one anymore.

### 2. Relay write success is not a sufficient success signal

You now have rows with **full relay success** and still **no voter observation**. That means:

* relay accepted the write
* but the voter-side live/backfill path still did not surface it

### 3. Backfill recovery is currently ineffective

`ticketObservedBackfillCount: 0` is the loudest signal in the whole report.

That means one of:

* backfill is not querying the right event set
* backfill is not being triggered often enough
* backfill results are not being reconciled correctly
* mailbox fetch / filter shape is still wrong for the new transport
* the object is not actually visible on the queried relays despite publish success

# Next phases

## Phase 9 - Attack backfill first

## Goal

Make backfill produce real recoveries.

## Why

Right now:

* live observation is weak
* resend helps a bit
* backfill helps essentially not at all

That makes backfill the highest-value next target.

## Required work

### 9.1 Instrument backfill attempts explicitly

For each row, add:

* `ticketBackfillAttemptCount`
* `ticketBackfillLastAttemptAt`
* `ticketBackfillLastResultCount`
* `ticketBackfillLastMatchedCount`
* `ticketBackfillLastSourceRelays`

You need to know whether backfill is:

* not being called
* returning zero events
* returning events that do not match
* returning events that do match but are ignored

### 9.2 Split backfill failure into exact classes

For each published-but-unobserved row, classify backfill as one of:

* `backfill_not_triggered`
* `backfill_no_events_returned`
* `backfill_events_returned_no_match`
* `backfill_match_found_not_reconciled`
* `backfill_match_found_observed`

### 9.3 Log exact query/filter shape used for backfill

For the ticket object, capture:

* kind
* tags used in filter
* mailbox id
* recipient `#p` if still used
* relay list queried
* time window queried

This is critical because your earlier state said mailbox fetch still had old `#p`-style coupling. If backfill is querying the wrong shape, it will never help.

## Exit condition

You get at least some rows classified as `backfill_match_found_observed`, not always zero.

---

## Phase 10 - Verify mailbox fetch semantics against actual published events

## Goal

Determine whether voter observation is failing because the read path is not aligned with the write path.

## Why

You now have:

* publish success
* no observation
* no backfill recovery

That often means the producer and consumer are not using exactly the same addressing/filter semantics.

## Required work

### 10.1 For one missed row, dump the exact published ticket envelope

Include:

* event id
* kind
* tags
* mailbox id
* request id
* ticket id
* relay targets
* relay success count

### 10.2 Dump the exact voter-side live filter

Include:

* kinds subscribed
* tags filtered
* mailbox id or `#p`
* relay set used

### 10.3 Dump the exact voter-side backfill filter

Same fields.

### 10.4 Compare them literally

You want to verify:

* published kind matches subscribed kind
* published mailbox tag matches queried mailbox tag
* time windows overlap
* relays queried include relays that accepted the publish

## Exit condition

You can answer:

* “the read path is querying the right thing”
  or
* “the read path is still mismatched to the mailbox write path”

This is probably the single most important concrete check now.

---

## Phase 11 - Promote mailbox-id keyed read path over legacy recipient-style fetch

## Goal

Align observation with the mailbox transport you already started implementing.

## Why

If the write path is mailbox-envelope-based but the read/backfill path still leans on legacy recipient fetch patterns, that would explain:

* publish success
* weak observation
* zero backfill recovery

## Required work

Where practical, make the voter read path prefer:

* mailbox kind
* mailbox id tag
* election id
* round id

over older recipient-only lookup patterns.

Do this first for:

* ticket observation
* ticket backfill

Do not try to finish the full Rust mailbox reducer tranche yet. Just align the read path with the mailbox write path.

## Exit condition

Backfill starts finding actual ticket events for some previously missed rows.

---

## Phase 12 - Test whether relay coverage mismatch is part of the problem

## Goal

See whether the relays the coordinator successfully published to are the same relays the voter is reading from.

## Why

You now have:

* `rowsWithFullRelaySuccessNoObservation > 0`

But “full relay success” only matters if the voter is actually connected to / backfilling from those relays.

## Required work

For each unmatched row, compare:

* `ticketRelayTargets`
* relays with write success
* relays the voter had live subscription on
* relays the voter used for backfill

Add summary counters:

* `rowsWhereWriteRelaysSubsetOfReadRelays`
* `rowsWhereWriteRelaysDisjointFromReadRelays`
* `rowsWhereBackfillQueriedWriteRelay`

## Exit condition

You know whether the observation problem is partly just relay-set mismatch.

---

## Phase 13 - Only after backfill works, refine resend

## Goal

Make resend a second-line recovery, not the only effective one.

## Why

Right now resend helps a bit, but backfill helps not at all.
That is upside down for a replayable relay system.

## Required work

Once backfill works:

* keep resend as the follow-up when backfill still finds nothing
* record whether recovery came from:

  * live
  * backfill
  * resend

Then compare effectiveness.

## Exit condition

You can say which recovery mode is actually carrying the system.

---

## Phase 14 - Re-test the operating point

## Goal

Once observation is fixed, determine whether `c5` is still the best default.

## Current read

From your current runs:

* `c5` looks best
* `c8` no longer shows the earlier anomaly, but still shifts failures toward observation
* longer windows help sends, not observation

So the likely best near-term default is still:

* `concurrency=5`
* longer windows while stabilising

But verify again after fixing read/backfill alignment.

---

# What I would do next, in order

1. **Instrument backfill failure classes**
2. **Dump exact published ticket envelope vs exact live/backfill filters**
3. **Check relay write/read set overlap**
4. **Prefer mailbox-id keyed fetch for ticket observation**
5. **Re-run `1 / 25 / 1`**
6. **Only then tune resend / concurrency again**

# Short summary

You are now in the **observation tranche** for real.

The next big question is no longer:

* “why aren’t tickets being sent?”

It is:

> **why do voters fail to observe tickets that were successfully published, and why does backfill recover none of them?**

That is the clearest next step from this report.
