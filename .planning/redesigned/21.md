This points to a **real bug**, and it is probably **not** in the blind-token crypto yet.

## What the results imply

### `1/1/1`

You got:

* `exitCode: 0`
* `completedRounds: 1`

but also:

* `questionnaireSeenCount: 0`
* `questionnaireOpenCount: 0`
* `responsesPublishedCount: 0`
* `definitionEventCount: 0`
* `resultEventCount: 0`
* `responseEventCount: 0`

That combination is internally inconsistent.

If the new questionnaire-first path had really executed, you should not get:

* `completedRounds: 1`
  with
* **zero observed questionnaire events**

So there is almost certainly a bug in one of these two places:

## Most likely bug A - harness marks completion without questionnaire truth

The harness is probably still incrementing `completedRounds` from a legacy success path or loop termination condition, even when no questionnaire stages were observed.

### Symptom

`completedRounds: 1` with all questionnaire counts zero.

### Fix

In `web/scripts/live-simple-relay-check.js`, in `course_feedback` mode:

* `completedRounds` must only increment when questionnaire-first completion criteria are met
* not when the run loop merely exits or the coordinator publishes some local state

### Correct completion rule

For `course_feedback` mode, a round should only count as completed if at least:

* `questionnaireSeenCount > 0`
* `questionnaireOpenCount > 0`

and ideally also:

* `definitionEventCount > 0`

For a stronger rule:

* `responsesPublishedCount >= expected`
* `resultEventCount > 0`

But the key bug is that **zero questionnaire events must never produce `completedRounds: 1`**.

---

## Most likely bug B - questionnaire transport observers are not actually wired to the live page

The `1/5/1` failure:

* `failureClass: protocol_timeout`
* `firstMissingStage: questionnaireSeen`

means the harness never observed the questionnaire become visible.

That could mean:

* questionnaire never published
* questionnaire published but not fetched
* questionnaire published/fetched but debug surface never updated
* harness is reading the wrong debug object / wrong field name

Given the earlier smoke run succeeded with questionnaire stage keys present, the most likely issue is now **wiring / readiness / observation**, not schema absence.

# Concrete files to inspect

## 1. `web/scripts/live-simple-relay-check.js`

Look for where:

* `completedRounds` increments
* `questionnaireSeenCount`
* `questionnaireOpenCount`
* `definitionEventCount`

are read and summarised

### Bug to fix

Any code like:

```js
completedRounds += 1;
```

based on:

* loop completion
* page survival
* legacy round state
* coordinator-side status only

must be replaced in `course_feedback` mode with questionnaire-stage-based completion.

### Add guard

In `course_feedback` mode:

```js
const questionnaireRoundComplete =
  questionnaireSeenCount > 0 &&
  questionnaireOpenCount > 0;
```

and do **not** mark a completed round unless that is true.

---

## 2. `web/src/QuestionnaireVoterPanel.tsx`

Check whether voter debug fields are actually being populated when:

* definition fetched
* questionnaire open
* response published

You want explicit fields like:

* `questionnaireSeen`
* `questionnaireOpen`
* `responsePublished`
* `definitionEventCount`
* `responseEventCount`

If these are only local component state and not exported into the debug object the harness reads, the harness will see zeros forever.

---

## 3. `web/src/questionnaireTransport.ts`

Check whether:

* fetch/subscribe actually runs in production/live harness mode
* relays are connected
* fetched questionnaire events are mapped into the voter/coordinator debug surfaces

A common bug is:

* transport module works
* UI renders
* but harness-facing debug counts are never updated

---

## 4. `web/src/SimpleUiApp.tsx`

Check the `course_feedback` branch.

Likely bug candidates:

* `deploymentMode` parsed correctly but questionnaire panel not mounted for some tabs
* questionnaire panel mounted but hidden behind another condition
* debug payload not updated from questionnaire state
* stale legacy “vote” tab state still controlling whether questionnaire logic runs

# Most likely exact bug to fix first

If I had to prioritise one fix first, it would be:

> **Fix `live-simple-relay-check.js` so `completedRounds` in `course_feedback` mode cannot become 1 unless questionnaire stages were actually observed.**

Because that is the clearest logical inconsistency in the results.

# Then next fix

After that, fix the observation path by ensuring the harness reads the correct questionnaire debug fields from:

* `QuestionnaireVoterPanel.tsx`
* `QuestionnaireCoordinatorPanel.tsx`

and that those fields are updated from `questionnaireTransport.ts`.

# Minimal patch intent

## In `live-simple-relay-check.js`

For `deploymentMode === "course_feedback"`:

* compute completion from questionnaire stages only
* never inherit legacy round completion logic
* if all questionnaire counts are zero, `completedRounds` must be zero

## In voter debug payload

Ensure these fields are exported and live:

* `questionnaireSeen`
* `questionnaireOpen`
* `definitionEventCount`
* `responsePublished`
* `responseEventCount`

# Short summary

The clearest bug is:

> **the harness is still able to report `completedRounds: 1` in `course_feedback` mode even though no questionnaire events were observed.**

That should be fixed first in:

* `web/scripts/live-simple-relay-check.js`

Then verify the questionnaire debug counters are actually being populated in:

* `web/src/QuestionnaireVoterPanel.tsx`
* `web/src/questionnaireTransport.ts`
* `web/src/SimpleUiApp.tsx`

