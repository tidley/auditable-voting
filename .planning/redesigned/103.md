## UI patch plan by file

Keep the current working questionnaire-first architecture. Only simplify and reorganise the UI.

---

## 1. `web/src/SimpleCoordinatorApp.tsx`

### Goal

Make the top-level coordinator tabs reflect the actual workflow.

### Changes

Rename tabs:

* `Configure` → **Questionnaire**
* `Voting` → **Results**
* `Settings` stays **Settings**

If you want to keep the old internal names, change only the visible labels.

### Recommended wording

Top tabs:

* **Questionnaire**
* **Results**
* **Settings**

Add a small persistent status strip near the top:

* **State:** Draft / Open / Closed
* **Responses:** `25 accepted, 0 rejected`
* **Mode:** Blind token
* **Payload:** Encrypted

### Also change

Pass coordinator identity props directly into the questionnaire panel, as you already do, and also pass:

* current accepted count
* rejected count
* questionnaire state
* payload mode

That keeps the panel self-contained.

---

## 2. `web/src/QuestionnaireCoordinatorPanel.tsx`

### Goal

Turn this into the main questionnaire builder and coordinator control panel.

### New section order

#### A. Questionnaire header

At the top of the panel, show:

* questionnaire title
* questionnaire id
* action buttons:

  * **Copy npub**
  * **Show QR**
  * **New ID** if still needed

Use the same QR/copy implementation as Settings.

### Recommended wording

* **Copy npub**
* **Show QR**

Do not use:

* `Scan` here
* technical labels around `npub` beyond the button text

---

#### B. Questionnaire details

Keep these fields together:

* **Questionnaire ID**
* **Title**
* **Description**
* **Close after (minutes)**

### Recommended wording

* **Questionnaire ID**
* **Title**
* **Description**
* **Close after (minutes)**

Button row:

* **Refresh**
* **Publish questionnaire**
* **Open questionnaire**
* **Close questionnaire**
* **Publish results**

Rename:

* `Publish definition` → **Publish questionnaire**
* `Set open` → **Open questionnaire**
* `Set closed` → **Close questionnaire**

---

#### C. Coordination

Move threshold here from Results/Voting.

Include:

* **Lead coordinator**
* **Sub-coordinators**
* **Threshold**

### Threshold behaviour

Default threshold to the number of included coordinators.

For now, with one coordinator:

* `1 of 1`

### Recommended wording

Section title:

* **Coordination**

Field labels:

* **Lead coordinator**
* **Sub-coordinators**
* **Threshold**

Helper text:

* **Default: all included coordinators**

If single-coordinator only:

* **Single coordinator mode: threshold is 1 of 1**

---

#### D. Questions

This becomes the main builder area.

Each question should be a card.

### Add controls

At top of section:

* **Add yes/no question**
* **Add multiple choice question**
* **Add free text question**

### Per-question card contents

* **Question 1**, **Question 2**, etc.
* **Type**
* **Prompt**
* **Required**
* type-specific settings

Buttons on each card:

* **Duplicate**
* **Move up**
* **Move down**
* **Delete**

### Multiple choice card

Show:

* list of options
* **Add option**
* toggle for:

  * **Allow multiple selections**

### Free text card

Show:

* **Maximum length**
* optional placeholder field if useful

### Recommended wording

Section title:

* **Questions**

Field labels:

* **Prompt**
* **Required**
* **Options**
* **Add option**
* **Allow multiple selections**
* **Maximum length**

Buttons:

* **Duplicate**
* **Move up**
* **Move down**
* **Delete**

Do not use:

* `Append`
* `Config box`
* technical wording

Keep it very plain.

---

#### E. Status

Move operational state into a compact section at the bottom.

Show:

* **Coordinator key loaded:** Yes / No
* **Latest state:** Draft / Open / Closed
* **Accepted responses:** N
* **Rejected responses:** N
* **Latest published result:** N accepted / none

### Recommended wording

Section title:

* **Status**

Rows:

* **Coordinator key loaded**
* **Latest state**
* **Accepted responses**
* **Rejected responses**
* **Latest published result**

---

#### F. Remove or hide legacy controls

Remove from the normal coordinator UI if not required:

* **No share indexes needed**
* **Republish blind key**

If still needed for debugging, put them under a collapsed section:

* **Advanced tools**

---

## 3. `web/src/QuestionnaireVoterPanel.tsx`

### Goal

Make the voter flow easy to follow for many questions.

### New section order

#### A. Questionnaire header

Show:

* title
* description
* state
* short privacy note

### Recommended wording

* **Questionnaire**
* **This response is submitted using a one-time token**
* **Answers are encrypted** or **Answers are public**, depending on mode

---

#### B. Response token status

Show a simple step block:

1. **Request token**
2. **Token received**
3. **Response ready**
4. **Submitted**

### Recommended wording

Buttons and states:

* **Request token**
* **Waiting for token**
* **Token ready**
* **Response submitted**

Keep it simple. Do not expose cryptographic jargon by default.

---

#### C. Questions

Render one long form with numbered questions.

Each question block:

* question number
* prompt
* input control
* validation message if needed

### Recommended wording

Validation:

* **This question is required**
* **Please select at least one option**
* **Answer is too long**

---

#### D. Final submit

At bottom:

* **Submit response**

Optional before submit:

* **Review your answers**

If you want minimum complexity, skip review for now and just keep:

* **Submit response**

---

#### E. Submission status

After submit:

* **Response submitted**
* **You can close this page**

---

## 4. `web/src/questionnaireRuntime.ts`

### Goal

Keep wording/state names consistent.

### Changes

Normalise display strings for questionnaire states:

Internal → UI:

* `draft` → **Draft**
* `open` → **Open**
* `closed` → **Closed**
* `results_published` → **Results published**

For token/admission status:

* `token_ready` → **Token ready**
* `response_submitted` → **Response submitted**
* `duplicate_nullifier` → **Duplicate response token**
* `invalid_token_proof` → **Invalid response token**

Use these user-facing labels consistently.

---

## 5. `web/src/QuestionnaireCoordinatorPanel.tsx` and `web/src/QuestionnaireVoterPanel.tsx`

### Goal

Use the same wording everywhere.

### Shared wording list

#### Questionnaire actions

* **Publish questionnaire**
* **Open questionnaire**
* **Close questionnaire**
* **Publish results**

#### Identity

* **Copy npub**
* **Show QR**

#### Question builder

* **Add yes/no question**
* **Add multiple choice question**
* **Add free text question**
* **Duplicate**
* **Move up**
* **Move down**
* **Delete**

#### Status

* **Coordinator key loaded**
* **Latest state**
* **Accepted responses**
* **Rejected responses**
* **Response submitted**
* **Token ready**

---

## 6. `web/src/SimpleCoordinatorApp.tsx` and Settings-related component

### Goal

Clarify restore behaviour.

### Change wording

If restore restores everything, label it:

* **Restore full local state**

If it restores only identity, label it:

* **Restore identity only**

### Recommended behaviour

Prefer:

* **Restore full local state**

and make it restore:

* coordinator identity
* questionnaire draft
* published questionnaire metadata
* coordination settings
* local runtime state where appropriate

If that is not what it currently does, do not imply it does.

---

## 7. Optional small layout improvements

These are low risk and worth doing.

### In coordinator panel

* collapse:

  * **Coordination**
  * **Sub-coordinators**
  * **Voters**
* keep **Questions** open by default

### In voter panel

* keep token section compact
* keep questions dominant
* keep submit button sticky at bottom if easy

---

## Summary of recommended visible wording

### Tabs

* **Questionnaire**
* **Results**
* **Settings**

### Coordinator actions

* **Copy npub**
* **Show QR**
* **Publish questionnaire**
* **Open questionnaire**
* **Close questionnaire**
* **Publish results**

### Question builder

* **Add yes/no question**
* **Add multiple choice question**
* **Add free text question**

### Status

* **Coordinator key loaded**
* **Latest state**
* **Accepted responses**
* **Rejected responses**
* **Latest published result**

### Voter flow

* **Request token**
* **Waiting for token**
* **Token ready**
* **Submit response**
* **Response submitted**

---

## Recommended implementation order

1. `SimpleCoordinatorApp.tsx`

   * rename tabs
   * add top summary strip

2. `QuestionnaireCoordinatorPanel.tsx`

   * reorder sections
   * move threshold here
   * add Copy npub / Show QR
   * replace single-question area with question list builder UI

3. `QuestionnaireVoterPanel.tsx`

   * simplify token flow wording
   * improve question layout
   * add clear submit section

4. Settings component

   * clarify restore wording

If you want, I can turn this into an even more literal patch checklist with exact section names and component substructure.

---

## Execution log (2026-04-14)

### Implemented

1. `web/src/SimpleCoordinatorApp.tsx`
   - renamed visible top tabs:
     - `Configure` -> `Questionnaire`
     - `Voting` -> `Results`
   - added persistent questionnaire summary strip under header:
     - `State`
     - `Responses`
     - `Mode`
     - `Payload`
   - wired status callback from `QuestionnaireCoordinatorPanel` to keep strip live.

2. `web/src/QuestionnaireCoordinatorPanel.tsx`
   - reorganised panel into questionnaire-first flow with plain wording.
   - added identity action row:
     - `Copy npub`
     - `Show QR` (toggle + `SimpleQrPanel`)
   - renamed actions:
     - `Publish definition` -> `Publish questionnaire`
     - `Set open` -> `Open questionnaire`
     - `Set closed` -> `Close questionnaire`
   - added `Coordination` section with single-coordinator threshold wording.
   - implemented question builder controls:
     - `Add yes/no question`
     - `Add multiple choice question`
     - `Add free text question`
     - per-question `Duplicate`, `Move up`, `Move down`, `Delete`
     - multiple-choice `Add option` + `Allow multiple selections`
     - free-text `Maximum length`
   - status section now uses wording:
     - `Coordinator key loaded`
     - `Latest state`
     - `Accepted responses`
     - `Rejected responses`
     - `Latest published result`

3. `web/src/QuestionnaireVoterPanel.tsx`
   - reordered to questionnaire-first layout:
     - questionnaire header/title/description/state note
     - compact token step section
     - long-form questions
     - final submit
   - simplified wording:
     - `Request token`
     - `Waiting for token`
     - `Token ready`
     - `Submit response`
     - `Response submitted`
   - added post-submit note:
     - `You can close this page.`

4. `web/src/questionnaireRuntime.ts`
   - added shared display label helpers:
     - `formatQuestionnaireStateLabel(...)`
     - `formatQuestionnaireTokenStatusLabel(...)`
   - mapped required wording for:
     - draft/open/closed/results_published
     - token_ready/response_submitted/duplicate_nullifier/invalid_token_proof

5. Settings wording clarity
   - `web/src/SimpleIdentityPanel.tsx`:
     - `Restore` -> `Restore identity only`
     - `Restore backup` -> `Restore full local state`
   - `web/src/SimpleCoordinatorApp.tsx` backup status copy updated to:
     - `Full local state restored from backup.`

### Verification

- `npx tsc --noEmit` passed.
- `npx vitest run src/questionnaireProtocol.test.ts src/questionnaireRuntime.test.ts src/questionnaireBlindToken.test.ts` passed.
