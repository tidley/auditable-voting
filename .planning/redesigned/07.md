Good. This is a meaningful shift.

You have now proved two things:

* **resend machinery is real**, not hypothetical
* at `1 / 25 / 1`, the dominant failure is now **upstream of observation** for most rows: `ticket_not_sent: 18`, with a smaller secondary class of `ticket_published_not_observed: 5`

So the next phases should no longer centre on generic recovery. They should split cleanly into:

1. **send scheduling / throughput**
2. **observation recovery for the rows that do get sent**

# Recommended next phases

## Phase 7 - Fix `ticket_not_sent` under 25-voter load

### Goal

Make far more rows reach at least `ticketPublishStarted`.

### Why

Your `1 / 25 / 1` run shows:

* only `2` tickets observed
* only `2` ballots accepted
* `23` unmatched rows
* dominant unmatched stage: **`ticket_not_sent: 18`**

That means the primary bottleneck is now **scheduler throughput / send initiation**, not acceptance, not mapping, and not even observation for most rows.

### Working hypothesis

One or more of these is limiting throughput:

* concurrency window too small for the current loop shape
* resend / retry rows are monopolising send slots
* send loop eligibility ordering is poor
* rows are remaining blocked in `retry_age_not_elapsed`
* a hidden gate still suppresses send-start for many rows
* round window ends before the scheduler drains the queue

### Required work

#### 7.1 Add explicit send-queue diagnostics

Expose at coordinator debug level:

* `sendQueueEligibleCount`
* `sendQueueStartedCount`
* `sendQueueBlockedCount`
* `sendQueueBlockedReasons` aggregate map
* `sendQueueInFlightCount`

For each row:

* `sendEligible`
* `sendBlockedReason`
* `sendAttempted`
* `sendStartedAt`

#### 7.2 Split first-send from resend scheduling

Right now resend is active, which is good, but for `1 / 25 / 1` the main problem is that many rows never get their **first** send at all.

Use two queues:

* **Queue A**: first-send rows
* **Queue B**: resend rows

Scheduling rule:

* always prioritise Queue A until every eligible voter has had one send attempt
* only then spend spare capacity on Queue B

Do not let resend rows starve unsent rows.

#### 7.3 Add “oldest unsent first” ordering

Sort first-send candidates by:

* round
* first request seen time
* stable voter row index

This makes the scheduler deterministic and easier to debug.

#### 7.4 Add a round-window pressure metric

At summary level expose:

* `roundOpenAt`
* `lastTicketSendStartedAt`
* `unsentRowsAtRoundTimeout`

This will tell you whether the round simply ends before the scheduler drains.

### Exit condition

In `1 / 25 / 1`, the dominant unmatched stage is no longer `ticket_not_sent`.

---

## Phase 8 - Tune first-send throughput before more resend tuning

### Goal

Get initial ticket dispatch to scale for 25 voters.

### Why

If 18 rows never even get a first send, resend logic cannot save them.

### Required work

#### 8.1 Experiment with concurrency

You now have env overrides. Use them.

Run `1 / 25 / 1` with:

* concurrency `3`
* concurrency `5`
* concurrency `8`

Keep other settings fixed.

#### 8.2 Experiment with longer round/ticket waits

For throughput diagnosis, increase:

* `LIVE_ROUND_WAIT_MS`
* `LIVE_TICKET_WAIT_MS`

This is not the final product setting. It is to determine whether you are compute/scheduler bound or truly transport bound.

Suggested diagnostic run:

* `LIVE_ROUND_WAIT_MS=60000`
* `LIVE_TICKET_WAIT_MS=60000`

If send counts rise sharply, the problem is scheduler/window pressure.
If they do not, the problem is eligibility/blocking logic.

#### 8.3 Collect per-run scheduler summary

For every run, record:

* first-send attempted count
* resend attempted count
* unsent row count
* average time to first send
* last first-send start time

### Exit condition

You know which concurrency/window settings materially improve first-send coverage.

---

## Phase 9 - Only after send throughput improves, attack `ticket_published_not_observed`

### Goal

Recover the rows that are sent but still not seen.

### Why

This is still real:

* `ticket_published_not_observed: 5` in `1 / 25 / 1`
* same class appeared in smaller runs too

But it is now secondary to `ticket_not_sent`.

### Required work

#### 9.1 Keep bounded backfill + resend

That framework is now functioning in `1 / 5 / 1`, which is good.

#### 9.2 Distinguish live vs backfill success in summaries

You already added this. Make it central in run summaries:

* `observedLiveCount`
* `observedBackfillCount`
* `recoveredByResendCount`

#### 9.3 Add resend-after-publish-success rule

If:

* publish succeeded
* no observation after threshold
  then:
* backfill
* resend same logical `ticket_id`
* mark whether recovery came from resend or backfill

### Exit condition

In `1 / 25 / 1`, sent-but-unobserved rows reduce materially.

---

## Phase 10 - Re-baseline small run after scheduler changes

### Goal

Make sure throughput fixes do not break the smaller path.

### Why

You now have `1 / 5 / 1` runs where resend engages but passes are still `0/3`. That means the scheduler/transport path is still fragile even before scale.

### Required work

After every meaningful scheduler change:

* rerun `1 / 5 / 1` at least 10 times
* compare:

  * `ticket_not_sent`
  * `ticket_published_not_observed`
  * `ticket_recovered_by_resend`
  * final accepted ballot count

### Exit condition

Small-run reliability improves, not regresses.

---

## Phase 11 - Define the first deployable threshold for course feedback

### Goal

Set an operational bar for the 1-coordinator deployment.

### Why

For this initial use case, perfect theoretical elegance matters less than:

* stable ticket dispatch
* accepted ballots
* zero silent protocol failures

### Suggested internal thresholds

For `1 / 25 / 1`:

* primary target: `25/25` accepted
* intermediate target: most rows at least sent, with remaining misses classified cleanly
* any run with many `ticket_not_sent` rows is still a scheduler failure, not acceptable

### What to report

Per run:

* `ticket_not_sent`
* `ticket_published_not_observed`
* `ticket_recovered_by_resend`
* `coordinatorAcceptedBallots`
* `coordinatorRejectedBallots`

---

# Concrete immediate task list

## Do next

1. Add explicit queue diagnostics and blocked-reason aggregates in `SimpleCoordinatorApp.tsx`
2. Split first-send queue from resend queue
3. Prioritise first-send rows over resend rows
4. Run `1 / 25 / 1` with concurrency `3/5/8`
5. Run `1 / 25 / 1` with longer waits to determine scheduler-window pressure
6. Re-run `1 / 5 / 1` after each change
7. Only then return to observation recovery tuning

# Short summary

The next highest-value work is exactly what you concluded:

> **attack `ticket_not_sent` first**

More precisely:

> **Fix the ticket scheduler so first sends drain for 25 voters before resends consume capacity, then revisit observation recovery for the tickets that are actually sent.**

That is the clearest next tranche from the current state.
