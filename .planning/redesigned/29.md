Excellent. That is the first convincing end-to-end result for the new architecture.

## What Phase 28 proves

For `1 / 5 / 1`, `course_feedback`, real relays:

* `passed = true`
* `completedRounds = 1`
* `roundSuccess = true`
* `questionnaireSeenCount = 5`
* `responsesPublishedCount = 5`
* `acceptedResponsesCount = 5`
* `resultSummaryPublished = true`

And the timeline is coherent:

* questionnaire publish attempted at `+59.8s`
* all 5 voters saw questionnaire by `+64.2s`
* questionnaire publish succeeded at `+68.0s`
* all 5 responses published by `+74.0s`
* result summary published at `+89.3s`

That means the **questionnaire-first blind-token path is now working end-to-end** under a small real-relay multi-voter run.

# What the actual bug was

The decisive fix here was not relay transport, crypto, or reducer logic. It was:

> **startup identity / participant identity reads in the harness were too brittle**

Once you hardened:

* coordinator `npub` read
* voter id read
* UI-first + IndexedDB fallback with timeout
* fail-fast on missing identity

the protocol ran cleanly.

So the architecture was less broken than it seemed; the test harness was mis-sequencing or misidentifying actors.

# What to do next

## 1. Freeze this as a known-good baseline

Do not immediately refactor this path.

Record this run profile as the current good baseline:

* `1/5/1`
* `course_feedback`
* real relays
* current timeout profile

## 2. Run repeats

Next highest-value step:

* repeat `1 / 5 / 1` at least 5-10 times

You want:

* pass rate
* stage timing spread
* whether all 5 accepted remains stable

## 3. Move to `1 / 25 / 1`

Once repeatability looks reasonable, go to the real target.

For `1 / 25 / 1`, track:

* `questionnaireSeenCount`
* `responsesPublishedCount`
* `acceptedResponsesCount`
* `resultSummaryPublished`
* first/last stage timings

## 4. Add duplicate-nullifier test

Because blind-token uniqueness is the core admission rule, add one adversarial run:

* two responses using same nullifier
* confirm first accepted, second rejected

## 5. Test encrypted payload mode separately

If the current passing path is public payload mode, next check:

* same questionnaire flow
* encrypted response payload
* coordinator decrypt + accept + summarise

# Recommended immediate milestone statement

You can now say:

> **The questionnaire-first blind-token protocol has passed a real-relay `1 coordinator / 5 voters / 1 questionnaire` end-to-end run, including questionnaire visibility, response publication, response acceptance, and result summary publication.**

That is a genuine milestone.

# Short summary

You have gone from:

* opaque timeouts and mixed architectures

to:

* a working real-relay blind-token questionnaire flow

The next steps are:

1. repeat `1/5/1`
2. run `1/25/1`
3. add duplicate-nullifier validation
4. test encrypted response payload mode

