It is not stagnant. It has narrowed.

You now have a much harder but much cleaner diagnosis:

* tickets are being **published**
* publish is often reported as **successful**
* voters still **do not observe** them
* backfill is being **triggered**
* backfill returns **no events**
* relay overlap is often **present**
* mailbox filter mismatch is now showing up explicitly, especially in the `1/5/1` smoke run

That means the bottleneck is no longer “general transport unreliability”. It is now much closer to:

> **the reader is still not fetching the same objects the writer successfully publishes**

or, slightly broader:

> **publish-side success semantics and read-side query semantics are not aligned**

## What this most likely means

Given:

* `coordinatorTicketPublishStartedCount: 5`
* `coordinatorTicketPublishSucceededCount: 5`
* `votersWithTickets: 0/25`
* `backfillFailureClassCounts: { backfill_no_events_returned: 25 }`
* `rowsWithRelayOverlapNoObservation: 5`
* `rowsWithNoRelayOverlapNoObservation: 0`

the strongest current hypothesis is:

### Hypothesis A

The mailbox ticket events are being written, but the voter-side fetch/subscription filter is still not querying the exact same tag/kind combination that was written.

### Hypothesis B

The events are being written to relays that acknowledge the write, but those relays are not actually serving those events back under the mailbox-tag query shape you now use.

### Hypothesis C

The published event contents/tags are internally inconsistent:

* mailbox id on write differs from mailbox id on read
* `etype` tag differs from what the reader asks for
* kind is right but tags are wrong
* or vice versa

The fact that backfill returns **no events**, rather than “events returned but no match”, is the big clue. That usually means:

* wrong filter
* wrong relays
* or relay policy/indexing mismatch

## Next phase

The next phase should be **not** another broad retry/recovery tranche.

It should be:

# Phase 10 - prove write/read equivalence

## Goal

For one missing voter row, prove whether the exact event that was published can or cannot be retrieved using the exact read filter.

## Why

Until you prove this at the single-event level, all larger runs will remain noisy.

## Required steps

### Step 1 - Capture one concrete published ticket event

For a single missing row, dump all of this from the coordinator side:

* event id
* kind
* all tags exactly as published
* mailbox id
* `etype`
* request id
* ticket id
* relay targets
* relay write success result per relay
* timestamp

This must be one concrete object, not an aggregate summary.

### Step 2 - Capture the exact voter-side live filter

For that same row, dump:

* relay list used for live subscribe
* kinds used
* tags used
* mailbox ids used
* `etype` values used
* time window / since cursor if any

### Step 3 - Capture the exact voter-side backfill filter

Same as Step 2, but for backfill.

### Step 4 - Compare literally

Check, field by field:

* published kind == subscribed/backfill kind
* published `mailbox` tag value == queried mailbox value
* published `etype` == queried `etype`
* at least one queried relay is a relay that acknowledged the write
* time window includes the published timestamp

### Step 5 - Manual fetch probe

For the same row, run one direct fetch against one relay that acknowledged the write using:

* only kind
* then kind + mailbox
* then kind + mailbox + etype

This tells you whether the relay is:

* storing the event
* indexing those tags
* returning it under the expected filter shape

## Exit condition

You can say one of these with proof:

* “The event is published correctly and the reader filter is wrong.”
* “The reader filter is correct, but the relay is not returning stored mailbox events under that filter.”
* “The mailbox id / etype written is inconsistent with what the reader expects.”
* “The event is never actually retrievable even from a relay that acknowledged it.”

That is the next real decision point.

---

# Phase 11 - collapse the query shape temporarily

## Goal

Remove query complexity and see whether the events become visible.

## Why

You just added `#mailbox + #etype` preference, but the smoke run still shows mailbox-filter mismatch signals. That is exactly when you simplify aggressively.

## Temporary diagnostic change

For one diagnostic branch only, make voter backfill fetch with a broader filter:

* mailbox kind only
* mailbox kind + election
* mailbox kind + round

Then reconcile locally by:

* mailbox id
* request id
* ticket id

In other words:

* **broaden fetch**
* **narrow match locally**

Do this only as a diagnostic mode, not necessarily as the final production query.

## Why this helps

If broad fetch suddenly finds the missing ticket events, then the problem is almost certainly:

* mailbox tag filter shape
* `etype` filter shape
* or relay tag indexing behaviour

## Exit condition

You know whether broad fetch can see the supposedly published mailbox tickets.

---

# Phase 12 - remove `etype` from the critical read path temporarily

## Goal

Test whether `etype` tagging is the thing breaking retrieval.

## Why

You now have explicit mailbox-filter mismatch counts. The simplest likely mismatch is often an extra tag restriction that is not necessary.

## Required change

For one test mode:

* keep mailbox kind
* keep mailbox id
* drop `etype` from the fetch filter
* reconcile by parsing content locally

If visibility improves, `etype` is likely the culprit or part of it.

## Exit condition

You know whether `etype` should remain in the critical live/backfill filter.

---

# Phase 13 - verify mailbox id derivation symmetry

## Goal

Make sure writer and reader derive the same mailbox id for the same voter and round.

## Why

This is the other most likely cause.

## Required checks

For one missing row, print on both sides:

* election id
* round id
* voter npub
* mailbox salt / mailbox derivation inputs if applicable
* derived mailbox id

Do this:

* at publish time
* at read/backfill time

## Exit condition

Mailbox ids are either identical, or you find the mismatch immediately.

---

# Phase 14 - test relay policy/indexing directly

## Goal

Check whether the relays are actually willing to return these custom mailbox events under your tag queries.

## Why

Write acknowledgement alone is not enough.

## Required probe

Use the exact relay that reported successful write and test:

1. fetch by event id if possible
2. fetch by kind only
3. fetch by kind + mailbox
4. fetch by kind + mailbox + etype

This is the simplest way to learn whether the relay:

* stored the event
* indexed the mailbox tag
* indexed the extra tags the way you expect

## Exit condition

You know whether the relay itself is the problem.

---

# Phase 15 - only after that, choose the next structural move

Once you know the outcome of Phases 10-14, the next direction becomes obvious:

### If broad fetch works but narrow fetch does not

Then fix the mailbox filter shape.

### If mailbox ids differ

Then fix mailbox derivation symmetry.

### If acknowledged relays do not return the events

Then you need a different relay policy / relay set for mailbox traffic.

### If events are returned but not reconciled

Then the local matcher is still wrong.

## What not to do next

Do not spend the next tranche on:

* more resend tuning
* more queue tuning
* proof bundles
* Rust mailbox reducers
* multi-coordinator scale

Not yet.

You now need **proof of write/read equivalence** before anything else.

## Short version

You are stuck at the right layer now.

The next tranche should be:

1. **capture one published ticket event exactly**
2. **capture one voter live/backfill filter exactly**
3. **compare them literally**
4. **run a broad-fetch diagnostic**
5. **verify mailbox id symmetry**
6. **probe relay return behaviour directly**

That is the shortest path out of the current plateau.
