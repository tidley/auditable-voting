Good. That was the right fix.

## What Phase 21 achieved

You removed a false positive in the harness:

* `completedRounds` now reflects **actual questionnaire-stage truth**
* not loop completion
* not legacy round semantics

And the smoke result is now internally consistent:

* `passed: false`
* `completedRounds: 0`
* `summaryRounds: 1`

So the harness is no longer lying about success.

## What this means now

You are back to a clean state:

* the questionnaire-first path is selected
* the harness is measuring the right thing
* but the questionnaire flow is still **not actually being observed as live**

That means the next work is no longer harness cleanup.

It is now:

> **why are questionnaire events not becoming visible to the voter/client path in the live run?**

# Next highest-value phase

## Phase 22 - prove questionnaire publish/read equivalence

You already did this style of work for mailbox events. Do the same here.

### Goal

For one questionnaire run, prove whether the questionnaire definition/state/result events are:

1. actually being published
2. retrievable from relays
3. being queried with the correct filter
4. being reflected into voter debug state

## Add exact diagnostics

### In coordinator-side questionnaire publish path

Capture and expose:

* `questionnaireDefinitionPublishAttempted`
* `questionnaireDefinitionPublishSucceeded`
* `questionnaireDefinitionEventId`
* `questionnaireDefinitionKind`
* `questionnaireDefinitionTags`
* `questionnaireDefinitionRelayTargets`
* `questionnaireDefinitionRelaySuccessCount`

Similarly for:

* questionnaire state/open event
* result summary event

### In voter-side questionnaire read path

Capture and expose:

* `questionnaireDefinitionLiveFilter`
* `questionnaireDefinitionBackfillFilter`
* `questionnaireDefinitionLiveResultCount`
* `questionnaireDefinitionBackfillResultCount`
* `questionnaireStateLiveResultCount`
* `questionnaireStateBackfillResultCount`

### In harness

Add a `phase22QuestionnaireProbe` block similar to your earlier mailbox probe:

* exact published questionnaire definition snapshot
* exact live reader filter
* exact backfill reader filter
* manual relay probes:

  * `kind_only`
  * `kind + questionnaire`
  * `kind + questionnaire + etype`

## Why

Right now you know the questionnaire stages are not observed. You do not yet know whether:

* publish is failing
* fetch filter is wrong
* relay indexing is not returning the event
* debug state is not being updated

# Most likely bug classes now

## A. Publish never actually happens

The coordinator path claims it published, but no event exists.

## B. Event exists, but read filter is wrong

This is very plausible given your earlier mailbox experience.

## C. Event exists and is fetched, but voter debug does not update

Also plausible if `QuestionnaireVoterPanel.tsx` is not reflecting transport results into exported debug fields consistently.

## D. State/open event is missing even if definition exists

Then `questionnaireSeen` may still remain false depending on your rules.

# Concrete next checks

## 1. Check `QuestionnaireCoordinatorPanel.tsx`

Make sure publish actions actually run in the live harness path and expose event ids/results.

## 2. Check `questionnaireTransport.ts`

Make sure:

* subscription starts
* backfill runs
* filters use the right `questionnaire_id`
* the same kinds/tags are used on both write and read

## 3. Check `QuestionnaireVoterPanel.tsx`

Make sure these fields are updated from transport truth:

* `questionnaireSeen`
* `questionnaireOpen`
* `definitionEventCount`
* `stateEventCount`
* `resultEventCount`
* `responsePublished`

# Recommended next task list

1. Add exact questionnaire publish snapshots on the coordinator side.
2. Add exact questionnaire live/backfill filter snapshots on the voter side.
3. Add a `phase22QuestionnaireProbe` block in the harness.
4. Run `1/1/1` again.
5. Decide from the probe whether the problem is:

   * publish failure
   * read filter mismatch
   * relay return mismatch
   * debug-state wiring

# Important sequencing

Do this on:

* `1 / 1 / 1`

first.

Not `1 / 5 / 1`.

Because now the harness truth is fixed, and `1 / 1 / 1` is the cleanest environment to isolate questionnaire publish/read equivalence.

# Short summary

Phase 21 fixed the measurement bug.

The next phase should answer:

> **Are questionnaire events being published and not read, or not published at all, or read but not reflected into debug state?**

That is the next real blocker.

