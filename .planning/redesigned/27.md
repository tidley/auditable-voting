Yes - do exactly that next.

Run a **Phase-26-style `1 / 5 / 1`** and return only:

* **timeline summary**
* **global timeline**
* **per-voter phase state**
* **coordinator phase state**
* **timeout snapshot analysis**

No extra interpretation until you have that artifact.

## Why this is the right next move

You now have the harness you needed:

* per-run timeline
* per-actor timelines
* final snapshot
* stage timing summary

And `1 / 1 / 1` already proved the questionnaire-first blind-token path can work end-to-end on real relays.

So the exact next question is now:

> **Where, in time and by actor, does `1 / 5 / 1` stall?**

That is precisely what the new timeline harness is for.

# What to request from the next run

Ask for these four sections only.

## 1. Timeline summary

Return:

* `coordinatorReadyAtMs`
* `questionnairePublishedAtMs`
* `questionnaireOpenPublishedAtMs`
* `firstVoterSawQuestionnaireAtMs`
* `lastVoterSawQuestionnaireAtMs`
* `firstEligibilityRequestAtMs`
* `lastEligibilityRequestAtMs`
* `firstBlindIssueAtMs`
* `lastBlindIssueAtMs`
* `firstResponsePublishedAtMs`
* `lastResponsePublishedAtMs`
* `firstResponseAcceptedAtMs`
* `lastResponseAcceptedAtMs`
* `resultSummaryPublishedAtMs`

If any are missing, show them as `null`.

## 2. Global timeline

Return the ordered event list, but only the important kinds:

* `questionnaire_publish_attempted`
* `questionnaire_publish_succeeded`
* `questionnaire_open_publish_attempted`
* `questionnaire_open_publish_succeeded`
* `questionnaire_seen`
* `questionnaire_open_seen`
* `eligibility_request_sent`
* `eligibility_request_received`
* `blind_issue_succeeded`
* `blind_issue_received`
* `token_ready`
* `response_publish_succeeded`
* `response_accepted`
* `response_rejected`
* `result_summary_publish_succeeded`

## 3. Per-voter state summary

For each voter, return just:

* `currentPhase`
* `questionnaireSeen`
* `questionnaireOpen`
* `eligibilityRequested`
* `blindIssueReceived`
* `tokenReady`
* `responsePublished`
* `responseAccepted`
* `responseRejected`

That will show immediately where the spread occurs.

## 4. Coordinator timeout/final snapshot

Return:

* `currentPhase`
* `questionnairePublishAttempted`
* `questionnairePublishSucceeded`
* `questionnaireOpenPublishAttempted`
* `questionnaireOpenPublishSucceeded`
* `eligibilityRequestsReceived`
* `blindIssuesSucceeded`
* `responsesSeen`
* `responsesAccepted`
* `responsesRejected`
* `resultSummaryPublishAttempted`
* `resultSummaryPublishSucceeded`

# What I expect this will reveal

The likely stall points are now one of:

1. **questionnaire visibility spread**

   * some voters never see questionnaire/open

2. **eligibility request spread**

   * some voters never send request or coordinator never sees it

3. **blind issue delivery spread**

   * coordinator issues tokens, but some voters never receive them

4. **response publish spread**

   * voters get token but do not publish

5. **acceptance spread**

   * responses published but not accepted or decrypted

The timeline run will tell you which one it is.

# Recommended instruction to the next run

Use the existing Phase 26 harness and report **only**:

* artifact path
* timeline summary
* per-voter current phase table
* coordinator final snapshot
* first missing common stage across voters

That keeps the result readable.

# Short summary

Yes - run the **Phase-26-style `1 / 5 / 1`** next.

That is the right move because you now need **stage-by-stage timing and actor-by-actor divergence**, not more architectural guessing.

