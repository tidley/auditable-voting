Excellent. That is the first clean end-to-end proof on the new path.

## What Phase 23 proved

You now have a successful real relay run for the questionnaire-first protocol:

* `passed = true`
* `completedRounds = 1`
* `roundSuccess = true`
* `questionnaireSeenCount = 1`
* `responsesPublishedCount = 1`
* `acceptedResponsesCount = 1`
* `resultSummaryPublished = true`
* `definitionEventCount = 1`
* `responseEventCount = 1`
* `resultEventCount = 1`

That is a proper protocol slice working end-to-end.

## The important technical lesson

The probe confirmed:

* `kind_only` finds the definition event
* `#questionnaire-id` filtered queries return `0`
* the broad-fetch fallback plus local `questionnaireId` reconciliation fixes it

So the earlier invisibility bug was indeed a **relay tag-index/filtering mismatch**, not a failure to publish.

That is a decisive finding.

# What this means now

You have a working rule of thumb for this protocol:

> **Do not trust narrow relay tag filters for questionnaire objects in the critical path.
> Fetch broadly enough, then reconcile locally by `questionnaireId` and event type.**

That should now guide the rest of the implementation.

# Recommended next phases

## Phase 24 - propagate the same fallback strategy everywhere in questionnaire mode

### Goal

Make all questionnaire reads robust in the same way the definition read now is.

### Apply broad-fetch + local reconciliation to:

* questionnaire definition fetch
* questionnaire state/open fetch
* questionnaire response fetch
* questionnaire result summary fetch

### Rule

Primary path may still try the narrow filter for efficiency, but:

* if narrow query yields zero
* fallback to `kind_only`
* then filter locally by:

  * `questionnaireId`
  * `event_type`
  * any other schema fields

## Exit condition

All critical questionnaire reads are resilient to relay tag-index quirks.

---

## Phase 25 - run `1 / 5 / 1` on the fixed questionnaire path

### Goal

See whether the new working `1 / 1 / 1` path scales to a small multi-voter case.

### Measure

At minimum:

* `questionnaireSeenCount`
* `questionnaireOpenCount`
* `responsesPublishedCount`
* `acceptedResponsesCount`
* `resultSummaryPublished`
* `definitionEventCount`
* `responseEventCount`
* `resultEventCount`

And also:

* whether all voters see the same questionnaire
* whether accepted response count equals expected count

## Exit condition

`1 / 5 / 1` either passes or fails in a new, smaller and cleaner way.

---

## Phase 26 - verify nullifier uniqueness / duplicate rule

Because the blind-token mode is the real target, now that the basic path works, test its core admission rule.

### Add one duplicate-nullifier test

For a controlled run:

* submit two responses with the same token nullifier
* confirm:

  * first valid one accepted
  * second rejected
  * result summary counts only one

### Add counters

Expose:

* `duplicateNullifierRejectedCount`
* `rejectedResponsesCount`
* explicit rejection reasons

## Exit condition

The one-use blind-token rule is proven in the live path.

---

## Phase 27 - test public vs encrypted response payload mode separately

You asked for the protocol to support:

* optionally public or encrypted response payload

Now that the core publish/read path works, test both.

### Public payload mode

* easiest debugging mode
* maximally verifiable

### Encrypted payload mode

* better fit for real feedback
* likely your intended target
* verify:

  * response publish works
  * coordinator decrypts
  * accepted response count correct
  * result summary still publishes

## Exit condition

You know whether encrypted mode introduces a new transport or validation problem.

---

## Phase 28 - scale to `1 / 25 / 1`

Once `1 / 5 / 1` is green or nearly green, move to the actual target:

* `1 coordinator`
* `25 voters`
* `1 questionnaire`

### Success criteria

For the first viable target, I would look for:

* questionnaire seen by all
* one response per voter
* accepted response count near expected
* result summary published
* duplicate / invalid counts explained

# What not to do now

Do **not** go back to:

* mailbox debugging
* MLS startup debugging
* blind-key publication debugging
* old round/ticket flow

You now have a working architecture slice on the correct path. Stay on it.

# Concrete immediate task list

1. Extend broad-fetch fallback + local reconciliation to:

   * state
   * response
   * result summary
2. Run `1 / 5 / 1`
3. Add duplicate-nullifier live test
4. Test encrypted response payload mode
5. Then run `1 / 25 / 1`

# Short summary

Phase 23 is the first solid proof that the **questionnaire-first blind-token path works end-to-end** on real relays.

The key architectural lesson is:

> **use broad fetch plus local reconciliation for questionnaire objects, because relay tag filtering is not reliable enough in the critical path.**

That is the right foundation to scale from `1 / 1 / 1` to `1 / 25 / 1`.

