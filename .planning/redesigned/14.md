Yes. The conclusion is now clean:

* `1 / 1 / 1` passes
* `1 / 5 / 1` fails
* failure bucket is consistently **`observation_failure`**
* `mlsJoinAttemptCount=0`
* `controlCarrierFetchLastCount=0`

So the next work should be entirely:

> **fix coordinator startup observation under load before touching mailbox work again**

## What this means

This does **not** look like:

* malformed join state
* bad post-join transition
* mailbox bug

It looks much more like:

* the coordinator never sees the control-plane bootstrap/join material under the `1 / 5 / 1` harness conditions
* or the observation path is never even actually attempted in time

The two strongest clues are:

* `startupJoinFailureBucket=observation_failure`
* `controlCarrierFetchLastCount=0`

That combination usually means:

* fetch/subscription for the coordinator carrier path is empty or not running when it should
* or it is querying the wrong relays / wrong filters / wrong time window

# Next phase: startup observation repair

## Phase 14 - force visibility of the coordinator control carrier path

### Goal

Prove whether the coordinator is:

1. publishing the bootstrap/control event
2. subscribing to the right relay/filter
3. backfilling the right relay/filter
4. receiving nothing because of filter/timing mismatch

## Required work

### 14.1 Capture the exact startup publish event

For the single coordinator startup path, log:

* control carrier event id
* kind
* tags
* created_at
* relay targets
* relay publish success count
* whether local echo happened
* whether the event is retrievable by `kind_only`

You want the same style of evidence you already got for mailbox writes.

### 14.2 Capture the exact startup live filter

Log:

* relays subscribed
* kinds used
* tags used
* since/until window
* subscription start time

### 14.3 Capture the exact startup backfill filter

Log:

* relays queried
* kinds used
* tags used
* since cursor
* result count

### 14.4 Add startup probe classification

For failed startup runs, add:

* `startupPublishAttempted`
* `startupPublishSucceeded`
* `startupObservedLive`
* `startupObservedBackfill`
* `startupProbeKindOnlyCount`
* `startupProbeFilteredCount`

## Exit condition

You can say exactly which of these is true:

* published, but not queried correctly
* published, query correct, relay returns nothing
* not published at all
* published and returned, but reducer never transitions

---

## Phase 15 - broaden startup fetch temporarily

### Goal

See whether the startup observation path is over-filtered, just like the mailbox path was.

## Required work

For startup/control carrier read:

### Primary path

Keep the intended precise filter.

### Fallback diagnostic path

If precise startup fetch returns zero:

* run `kind_only`
* then `kind + election`
* then `kind + election + group`

Reconcile locally.

This should be diagnostic first, not permanent.

## Exit condition

You know whether precise startup filter is the thing causing `observation_failure`.

---

## Phase 16 - verify relay overlap for startup control carrier

### Goal

Make sure the coordinator is reading from relays that actually acknowledged the startup/control publish.

## Required work

For each failed startup run, compare:

* write-success relays
* live-read relays
* backfill-read relays

Add summary fields:

* `startupRelayOverlap`
* `startupRelayNoOverlap`
* `startupWriteRelayQueriedByBackfill`

## Exit condition

You know whether the coordinator is simply publishing to relays it is not reading back from.

---

## Phase 17 - add forced startup backfill retry

### Goal

Make startup observation less timing-sensitive under `1 / 5 / 1`.

## Required work

If coordinator remains:

* `mls_join_pending`
* `group_ready=false`
* `joined_group=false`

for a short threshold, then force:

1. one bounded startup backfill
2. one broader diagnostic fetch
3. one state replay
4. re-check `group_ready`

This is analogous to what you already did for ticket recovery, but for startup control events.

## Exit condition

At least some `1 / 5 / 1` runs recover from startup observation failure instead of timing out.

---

## Phase 18 - keep mailbox work frozen until startup is green again

### Goal

Avoid mixing layers.

Since `1 / 5 / 1` is still blocked at startup, any mailbox diagnostics after that are contaminated.

So:

* do not start Phase 13 mailbox fallback yet
* do not change mailbox filters again
* do not tune ticket resend again

until `1 / 5 / 1` reliably reaches `roundSeen`.

# Exact order to follow

1. **Log exact startup publish event**
2. **Log exact live and backfill startup filters**
3. **Add `kind_only` / broader startup probe**
4. **Compare write relays vs read relays**
5. **Add forced startup backfill retry**
6. **Repeat `1 / 5 / 1` until startup either passes or is proven broken in one exact place**
7. **Only then resume mailbox validation**

# Short summary

Your decision is right.

The next step is:

> **fix startup observation under load (`observation_failure`) before any more mailbox fallback work.**

The concrete first move is to treat startup exactly the way you treated mailbox transport:

* capture one published control event
* capture exact read filters
* compare them literally
* add broad fallback fetch
* classify where observation fails

