That run does **not** tell you anything new about the mailbox work.

It tells you you are blocked again by a **startup-class failure**:

* `protocolFailureClass = startup`
* first missing stage = `roundSeen`
* coordinator stuck at:

  * `mls_join_pending`
  * `waiting_for_group_ready`
  * `group_ready = false`
  * `joined_group = false`

So the next move is **not** Phase 13 mailbox fallback yet.

# Immediate conclusion

You currently have a **hard prerequisite failure**:

> the coordinator is not joining / forming the control group reliably enough to even reach round formation.

Until that is stable, mailbox validation remains blocked by upstream noise.

# What to do next

## Phase S1 - isolate startup separately from mailbox work

Freeze mailbox changes for the moment.

Run only the smallest startup-focused diagnostic:

* `1 coordinator`
* `1 voter`
* `1 round`

Goal:

* prove whether `mls_join_pending` can clear reliably at minimal load

If `1 / 1 / 1` also hits `mls_join_pending`, the problem is clearly startup/control-plane only.

If `1 / 1 / 1` passes but `1 / 5 / 1` fails, then startup is load-sensitive.

---

## Phase S2 - add explicit startup join diagnostics

For the coordinator, expose and log:

* `mls_join_started_at`
* `mls_join_last_attempt_at`
* `mls_join_attempt_count`
* `group_ready`
* `joined_group`
* `engine_status.readiness`
* last relay write/read result for coordinator carrier path
* whether a group bootstrap event was published
* whether that bootstrap/join event was observed back

You need one concrete answer to:

> is the coordinator failing to **publish** join/bootstrap state, or failing to **observe** it back?

---

## Phase S3 - classify `mls_join_pending` into one of three buckets

For each failed startup run, decide whether it is:

### A. publish failure

Coordinator never successfully publishes the group/bootstrap/join event.

### B. observation failure

Publish succeeded, but coordinator never sees the group state back.

### C. state transition failure

Publish and observation happened, but local state never flips `group_ready/joined_group`.

That classification will stop you from poking blindly at startup.

---

## Phase S4 - run repeated startup-only baseline

Before any more mailbox work, run repeated small startup checks:

* `1 / 1 / 1`
* then `1 / 5 / 1`

Track:

* passes
* stuck at `mls_join_pending`
* time to `joined_group=true`
* time to `group_ready=true`

You need to know whether startup is:

* generally flaky
* or only occasionally noisy

---

# What not to do next

Do **not**:

* start Phase 13 fallback fetch yet
* tune mailbox filters more
* change ticket resend again
* interpret this run as mailbox regression

This run never reached the mailbox plane.

# Practical decision rule

## If `1 / 1 / 1` fails at `mls_join_pending`

Focus entirely on startup/control-plane join.

## If `1 / 1 / 1` passes but `1 / 5 / 1` fails

Startup is interacting with timing/load; still fix startup first, then return to mailbox validation.

## If you get one clean `1 / 5 / 1` startup run

Then resume mailbox validation and inspect:

* `rowsWithMailboxIdMismatchNoObservation`
* `mailboxIdConsistentReadRequest`
* `mailboxIdConsistentBackfillRequest`

# Short answer

This run is a **startup blocker**, not a mailbox result.

So the next step is:

> **stabilise or classify `mls_join_pending` first, using a tiny startup-only baseline, before continuing mailbox-read diagnostics.**
