Good tranche. You now have what you needed most: **the transport bottleneck is measurable with real evidence** rather than inferred from symptoms.

The important new facts are:

* send concurrency and retry limits are now explicit
* ticket publish start/success is now visible
* live vs backfill observation is now visible
* unmatched rows now carry enough fields to classify failure mode properly
* despite that, the sample `1 / 5 / 1` repeat still went **0/3**, with `ticket_not_observed` still dominant, and `ticketResentCount` staying **0** in the sampled runs

That last point is the most important. It means:

> **you have added the retry framework, but in the sampled runs the recovery path is not actually engaging in a meaningful way**

So the next tranche should no longer be “more diagnostics”. It should be:

# Phase 6 - Make ticket recovery actually happen

## Goal

Convert the current recovery instrumentation into real recovery behaviour.

## Why

The current evidence says:

* tickets are being built
* publish starts
* publish often succeeds
* some tickets are still not observed
* retry count stayed at `0` in sampled runs

So either:

* the retry trigger is too conservative
* the resend scheduler is not reached before the run ends
* `ticketStillMissing` is set but does not flow into resend
* backfill is not counting as enough evidence to retry
* round timing closes the window before recovery occurs

All of those are now testable.

## Required work

### 6.1 Audit the resend trigger path

In `SimpleCoordinatorApp.tsx`, trace the exact conditions required for:

* `ticketStillMissing = true`
* `ticketLastBackfillAttemptAt` set
* resend attempt scheduled
* `ticketResentCount += 1`

Add one temporary debug field per row:

* `resendEligible`
* `resendBlockedReason`

Allowed block reasons should be explicit, for example:

* `publish_not_confirmed`
* `backfill_not_attempted`
* `retry_cap_reached`
* `retry_age_not_elapsed`
* `round_closing`
* `ticket_already_observed`

### 6.2 Lower the effective recovery activation threshold for test runs

For the next tranche, make recovery happen sooner in the test configuration.

Add environment overrides, for example:

* `SIMPLE_TICKET_RETRY_AGE_MS`
* `SIMPLE_TICKET_SEND_MAX_CONCURRENCY`
* `SIMPLE_TICKET_RETRY_MAX_ATTEMPTS`

Then run with:

* retry age lower than current `10000ms`, for example `3000-5000ms`
* same retry cap or slightly higher for experiments only

Do not hardcode aggressive values permanently. Make them tunable.

### 6.3 Force a backfill-then-resend sequence

For each row where:

* `ticketPublishSucceededAt` exists
* no live/backfill observation after threshold

do exactly:

1. bounded backfill
2. if still missing, resend same logical ticket lineage
3. stamp `ticketResentCount`
4. record second publish start/success
5. allow later live or backfill observation to reconcile

The resend must keep the same logical lineage:

* same `request_id`
* same `ticket_id`

## Exit condition

In repeated `1 / 5 / 1`, at least some previously missing tickets are recovered by actual resend/backfill, not just diagnosed as missing.

---

# Phase 7 - Split transport failure into two exact classes

## Goal

Stop grouping all ticket misses together.

## Why

Your latest note already suggests mixed cases:

* `ticket_not_observed`
* plus cases where publish is not yet confirmed but voter-side observation context exists

That means one label is now too coarse.

## Required new failure classes

Replace or expand `ticket_not_observed` into:

### A. `ticket_publish_unconfirmed`

Use when:

* `ticketPublishStartedAt` exists
* `ticketPublishSucceededAt` does not exist
* voter has not observed ticket

### B. `ticket_published_not_observed`

Use when:

* `ticketPublishSucceededAt` exists
* voter has neither live nor backfill observation

### C. `ticket_observed_unmapped`

Use when:

* voter-side observation exists
* but row still appears unmatched

### D. `ticket_recovered_by_backfill`

Use when:

* ticket not seen live
* but later seen via backfill

### E. `ticket_recovered_by_resend`

Use when:

* initial attempt missed
* later resend results in observation

## Exit condition

Repeat harness output can tell you which of those classes is dominant.

---

# Phase 8 - Decide whether relay success semantics are trustworthy

## Goal

Answer a very specific question:

> Does `publish succeeded` mean anything useful for voter observation in your current relay topology?

## Why

You now have relay target and success counts per unmatched row. Use them.

## Required analysis

For each unmatched row, compare:

* `ticketRelayTargets`
* `ticketRelaySuccessCount`
* `ticketObservedLiveCount`
* `ticketObservedBackfillCount`

Create a per-run summary:

* rows with publish success but no observation
* rows with partial relay success and no observation
* rows with publish unconfirmed but eventual observation
* rows with observation only after backfill

## Interpretation

If many rows show:

* publish success
* zero observation

then relay write acknowledgement is not a strong enough success signal and you should rely more heavily on backfill/retry.

If many rows show:

* publish unconfirmed
* later observed anyway

then the publish-confirmation policy is too strict or too slow relative to actual propagation.

## Exit condition

You know whether to optimise:

* relay write confirmation policy
* observation recovery
* or both

---

# Phase 9 - Tune concurrency only after recovery starts working

## Goal

Do not guess concurrency settings before the resend path is proven active.

## Why

Right now:

* max concurrency is set
* retry cap is set
* but sampled runs showed `ticketResentCount = 0`

So concurrency tuning is premature if recovery is not yet firing.

## Required work

Only after Phases 6-8:

* compare `MAX_CONCURRENCY = 1, 2, 3`
* compare retry age windows
* compare observation success rate
* compare end-to-end accepted ballots

For `1 coordinator / 25 voters / 1 round`, I would test:

* concurrency 1
* concurrency 2
* concurrency 3

and hold everything else constant.

## Exit condition

You have a measured best setting, not a guessed one.

---

# Phase 10 - Re-establish a clean small baseline, then rerun 1 / 25 / 1

## Goal

Make small-run reliability acceptable before treating `1 / 25 / 1` as meaningful.

## Required work

### 10.1 Repeat `1 / 5 / 1`

Run at least 10-20 repeats after recovery is actually firing.

Measure:

* pass rate
* dominant failure class
* recovery-by-backfill count
* recovery-by-resend count

### 10.2 Then run `1 / 25 / 1`

Use the same metrics.

For this course-feedback deployment, the key success fields should be:

* `voterObservedTickets`
* `voterPublishedBallots`
* `coordinatorAcceptedBallots`
* `coordinatorAcceptedByLineage`
* `rowsWithoutAcceptedBallotCount`
* unmatched stage distribution
* recovered-by-backfill count
* recovered-by-resend count

## Exit condition

`1 / 25 / 1` becomes a quantified transport-reliability problem rather than a vague delivery failure.

---

# Phase 11 - Defer everything else

Do **not** spend the next tranche on:

* proof bundles
* receipt commitments
* full Rust mailbox reducers
* multi-coordinator scale
* threshold tally

Those are not the current bottleneck.

---

# Literal next task list

## Next task 1

In `SimpleCoordinatorApp.tsx`, add:

* `resendEligible`
* `resendBlockedReason`

## Next task 2

Make resend trigger actually fire after:

* publish success or publish uncertainty
* failed observation after bounded backfill
* retry age threshold

## Next task 3

Add environment-configurable retry-age tuning.

## Next task 4

Expand harness failure labels into:

* `ticket_publish_unconfirmed`
* `ticket_published_not_observed`
* `ticket_observed_unmapped`
* `ticket_recovered_by_backfill`
* `ticket_recovered_by_resend`

## Next task 5

Run repeat `1 / 5 / 1` again and verify:

* `ticketResentCount > 0` occurs in failure-recovery cases

## Next task 6

Only then re-run `1 / 25 / 1`.

---

# Short summary

You have completed the **measurement** tranche.

The next tranche must be the **actual recovery** tranche.

Right now the system can tell you tickets are missing, but in the sample runs it is still not actually recovering them. The clearest next goal is:

> **Make missing tickets trigger bounded backfill and real resend, then classify whether recovery succeeds.**

That is the highest-value next step from the current state.
