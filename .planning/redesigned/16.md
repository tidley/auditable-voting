Yes - that is a real breakthrough.

## What changed

You have removed the wrong blocker for the initial deployment shape:

* `1 coordinator` no longer waits in `mls_join_pending`
* single-coordinator mode now bypasses MLS startup/observation correctly
* startup control fetch loops are no longer the thing timing out

And the evidence is clean:

* `coordinatorPhase: blind_key_publish_pending`
* `coordinatorGroupReady: true`
* `coordinatorJoinedGroup: true`
* `startupJoinFailureBucket: null`
* `controlCarrierFetchAttemptCount: 0`

So Phase 15 did its job.

## What the new blocker is

The timeout has moved **downstream of startup**.

The next hard blocker is now:

> **`blind_key_publish_pending`**

That means the next tranche should not go back to MLS or mailbox fetch first. It should focus on the **single-coordinator round-open / blind-key publication path**.

# Recommended next phases

## Phase 16 - isolate blind-key publication

### Goal

Answer one exact question:

> Is the blind key failing to be **published**, or published but not **observed back / reflected into readiness**?

### Add exact debug fields

For the single coordinator, expose:

* `blindKeyPublishAttempted`
* `blindKeyPublishStartedAt`
* `blindKeyPublishSucceeded`
* `blindKeyPublishSucceededAt`
* `blindKeyObservedBackLocally`
* `blindKeyPublishAttemptCount`
* `blindKeyPublishLastError`
* `blindKeyEventId`
* `blindKeyRelayTargets`
* `blindKeyRelaySuccessCount`

### Classification

Every blind-key stall should classify as one of:

* `blind_key_not_attempted`
* `blind_key_publish_unconfirmed`
* `blind_key_published_not_observed`
* `blind_key_published_and_observed_but_not_applied`

## Exit condition

You know exactly which of those four is happening.

---

## Phase 17 - bypass blind-key re-observation in single-coordinator mode if not needed

### Goal

Apply the same simplification logic you used for MLS startup.

### Why

For `1 coordinator`, if the coordinator itself generates and publishes the blind key, you may not need to block on a separate observation loop before proceeding.

### Rule to consider

If all are true:

* single-coordinator mode
* blind key generated locally
* publish succeeded to at least one required relay
* local state already has the blind key material

then set:

* `blind_key_publish_safe = true`

without waiting for an extra relay round-trip.

This is analogous to the single-coordinator MLS bypass:

* do not wait for distributed confirmation when there is no distributed coordinator state to converge

### Caution

Only do this if public audit still gets the event published normally.
You are bypassing **blocking on observation**, not bypassing publication.

## Exit condition

`1 / 5 / 1` reaches `roundSeen`.

---

## Phase 18 - if blind-key must still be observed, probe it exactly like mailbox events

### Goal

Use the same write/read equivalence method that worked for mailbox diagnosis.

### Do this

For one stalled run, capture:

* exact published blind-key event

  * kind
  * tags
  * created_at
  * event id
  * relay results
* exact live reader filter
* exact backfill reader filter
* manual relay probes:

  * `kind_only`
  * `kind + election`
  * `kind + election + round`

### Why

If blind-key publication is suffering the same pattern as mailbox tickets, you will find out immediately.

## Exit condition

You know whether blind-key stall is:

* publish failure
* filter mismatch
* relay return mismatch
* local state application bug

---

## Phase 19 - once round opens, return to mailbox observation

### Goal

Resume the ticket-plane work only after the round actually forms.

Once `1 / 5 / 1` gets past:

* `blind_key_publish_pending`
* and reaches `roundSeen`

then immediately re-check:

* `rowsWithMailboxIdMismatchNoObservation`
* `rowsWithMailboxFilterMismatchNoObservation`
* `backfillFailureClassCounts`
* `votersWithTickets`

Because only then does the Phase 12 mailbox work become meaningfully testable again.

---

# Most likely best next move

For the initial deployment, I would try this first:

## Single-coordinator blind-key fast path

In single-coordinator mode:

* if blind key generation succeeded locally
* and publish succeeded to at least one relay
* do not keep the coordinator stuck in `blind_key_publish_pending`

Advance to round-open readiness.

That is the same engineering principle that just fixed startup.

# Short summary

Phase 15 worked.

The new bottleneck is now clearly:

> **single-coordinator blind-key publication / observation gating**

So the next tranche should be:

1. **instrument blind-key publication exactly**
2. **classify publish vs observe vs apply**
3. **preferably bypass blind-key re-observation as a blocking prerequisite in single-coordinator mode**
4. **then return to mailbox/ticket observation once `roundSeen` is actually reached**

If you paste the relevant `blind_key_publish_pending` conditional from `SimpleCoordinatorApp.tsx`, I’ll rewrite the exact fast-path patch.

