That is real progress.

## What Phase 19 achieved

You have now broken the false dependency on the old round/ticket flow for `course_feedback` mode.

The important signals are:

* voter UI now branches directly into questionnaire-first mode
* harness now evaluates questionnaire stages, not `roundSeen`
* smoke harness run completed with:

  * `completedRounds: 1`
  * stage keys:

    * `questionnaireSeen`
    * `questionnaireOpen`
    * `responsePublished`
    * `resultSummaryPublished`

So the project is finally exercising the **new protocol path**, not timing out in the old one.

## Why this matters

Previously, a lot of debugging energy was being spent on startup / blind-key / mailbox / round gating that was no longer the right execution path for the questionnaire MVP.

Now you have:

> **a live end-to-end path for the questionnaire-first architecture**

That is the real milestone.

# What to do next

## Phase 20 - verify correctness of the new questionnaire path

Now that the correct runtime path is active, the next job is not more routing fixes. It is correctness.

### Check these explicitly

For one real harness run, confirm:

* questionnaire definition published exactly once
* questionnaire open event/state visible to all voters
* each voter can see the same questionnaire id and question count
* each voter publishes one response
* coordinator receives and decrypts/validates responses
* result summary is computed from actual accepted responses
* published result summary matches local recomputation

## Phase 21 - add blind-token admission truth to harness

Right now the stage list shows:

* `questionnaireSeen`
* `questionnaireOpen`
* `responsePublished`
* `resultSummaryPublished`

That is good for flow, but not yet enough for the blind-token protocol.

### Add these stage metrics

* `eligibilityRequested`
* `blindIssueReceived`
* `tokenReady`
* `responseAccepted`
* `responseRejected`
* `duplicateNullifierRejected`

Those are the real protocol-health stages for blind-token mode.

## Phase 22 - verify nullifier uniqueness rule

This is the key cryptographic admission rule now.

For repeated or adversarial tests, confirm:

* first valid response per nullifier wins
* second response using same nullifier is rejected
* different ephemeral pubkeys with same nullifier are still rejected
* result summary counts only accepted nullifiers

## Phase 23 - test public vs encrypted payload mode explicitly

If encrypted responses are now the intended MVP path, test these separately:

### Public payload mode

* easiest to inspect correctness
* useful for debugging
* fully publicly verifiable

### Encrypted payload mode

* more realistic for feedback privacy
* verify decryption, validation, and summary path
* check that public summary still matches coordinator tally

If both are too much at once, keep public payload mode as the debugging baseline and encrypted mode as the real target.

## Phase 24 - scale gradually

Now that `1/1/1` smoke works on the correct path, step up carefully:

1. `1 / 5 / 1`
2. `1 / 10 / 1`
3. `1 / 25 / 1`

For each step, measure:

* questionnaire seen count
* eligibility/token readiness count
* response published count
* accepted response count
* rejected response count
* result summary published
* summary matches recomputation

# Most important next question

The key thing you need to answer next is:

> **Is the blind-token questionnaire flow merely running, or is it enforcing the right acceptance rules correctly?**

That is the next real milestone.

# Recommended immediate next task list

1. Run a real `1 / 5 / 1` harness in `course_feedback` mode on the new path.
2. Add `eligibilityRequested`, `blindIssueReceived`, `tokenReady`, and `responseAccepted` counters to the harness.
3. Verify result summary is derived from accepted responses, not merely published responses.
4. Add one duplicate-nullifier test case.
5. Then move to `1 / 25 / 1`.

# Short summary

Phase 19 fixed the architectural routing bug.

You are now finally testing the **intended questionnaire-first protocol**, not the obsolete round/ticket path.

The next work should focus on:

> **blind-token correctness, response acceptance truth, and scaling the new flow from 1/1/1 to 1/25/1.**

