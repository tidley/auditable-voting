all:
  hosts:
    tollgate-vps:
      ansible_host: "{{ vps_ip }}"
      ansible_user: "{{ ansible_user | default('root') }}"
  children:
    voting_servers:
      hosts:
        tollgate-vps:
    mint_hosts:
      hosts:
        tollgate-vps:
